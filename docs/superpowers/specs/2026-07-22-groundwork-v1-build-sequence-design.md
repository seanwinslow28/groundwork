# groundwork V1 — build-sequence design

> **Workbench artifact, not product content.** This is the plan for scaffolding groundwork V1 from the locked wayfinder decisions. It lives under `docs/superpowers/` (build-workbench, distribution-safe per #10 manifest-scoping) and is superseded by the implementation plan it feeds. Date: 2026-07-22.

## Context

The wayfinder charting phase is complete: all 19 decision tickets on the [map](https://github.com/seanwinslow28/groundwork/issues/1) are resolved and recorded, with ubiquitous language captured in [CONTEXT.md](../../../CONTEXT.md). The way from the approved [design brief](https://github.com/seanwinslow28/code-brain/blob/376dd5af85cd25fe06bcbe3fc9c366c022a94332/docs/plans/2026-07-15-company-os-design-brief.md) (§6 multi-harness/work-packages, §7 scope ladder, §10 target layout) to a buildable V1 is charted. This document sequences the build.

**V1 target (brief §7):** adoptable in an afternoon — the binding constraint is the adopter's *required path* (interview → generate → validate → provision), not repo size. The committed layout (§10): `README.md`, `AGENTS.md`, one-line `CLAUDE.md`, `.cursor/rules/`, `interview/`, `ontologies/`, `skills/`, `governance/`, `delivery/`, `demo/`, `your-company/`, `scripts/validate.py`, `docs/`.

## The organizing insight

The three candidate starting points — `scripts/validate.py`, `demo/`, `interview/` — are not independent. They are all **consumers of one thing that does not yet exist as files: the schema.** Every CONTEXT.md decision is a *shape* (ontology tiers #5, Owner's Card spine #6, memory frontmatter #7, proposal schema #17, version pin #21, demo canon #16). The validator *checks* that shape; the demo *instantiates* it; the interview *generates* it. Each candidate, started whole and alone, fails differently:

- **Validator alone → inert.** A checker with nothing to check passes vacuously; a correct checker is indistinguishable from a broken one. (The wwf5d dead-path / corpus-void trap — a scan that scans nothing ships looking done.)
- **Demo alone → too big.** A complete synthetic company depends on *every* schema at once — maximum surface, zero checker feedback.
- **Interview alone → premature.** The generator produces content in the schema's shape; building it before the target shape is hand-proven means authoring a machine for an artifact never made once.

**Resolution: build one vertical slice (a walking skeleton).** One function, all the way down — schema-as-files + a filled demo instance + the validator checks that govern exactly those files — then repeat horizontally, then build the generator, then honesty-gate the outward docs.

## Decisions locked in this session

| # | Decision | Choice | Rationale (short) |
|---|----------|--------|-------------------|
| D1 | First increment | **Vertical slice** (one function, schema + validator + demo instance together) | De-risks the `schema→files` translation everything inherits; later work is horizontal repetition |
| D1a | Slice function | **People/HR → onboarding-orchestration** (automate-path, #4) | Max schema surface in one slice (common core + Substrate/Shape/Describability Gate); front-loads the trust/governance story |
| D2 | Charting `CLAUDE.md` → product root files | **Two-move split** | *Move 1 now:* migrate build rules to `docs/agents/build-sessions.md`, retire charting language. *Move 2 at end of Phase 1:* author product `AGENTS.md`, collapse `CLAUDE.md` to `@AGENTS.md`. Two audiences (workbench vs product), honesty-safe |
| D3 | README sequencing | **Tiered** (now / early / trailing) | Facts-about-the-world go in now; claims-about-our-software go in when true |
| D4 | Build-session machinery | **Lightweight gate**; "one increment ends green" | Mirrors the proven charting cadence; Fable 5 builds, Codex reviews, Sean merges |

**Confirmed, not reopened:** Fable 5 + Codex review gate holds (brief §9, CLAUDE.md). This planning session runs on Opus + wwf5d (planning is what wwf5d is for); build sessions run on Fable 5 directly. Codex is the review gate; the **commit bit** (Sean's merge) is the actual governance teeth (#18).

## Dependency order (the spine → consumers)

1. **Schema-as-files** (the grammar): ontology worksheet template, work-package spec, Owner's Card template, org-memory schema, constitution/governance templates → `ontologies/ skills/ governance/`.
2. **`scripts/validate.py`** — co-developed; each schema file gets its checks in the same slice (can't trust a shape without a check, can't write a check without a shape).
3. **One demo function slice** — the end-to-end vertical proof.
4. **Rest of `demo/`** — horizontal repetition.
5. **`interview/`** — the generator, now that the output shape is proven.
6. **`delivery/` + `docs/` + README capability claims** — honesty-gated, last.
7. **Root files** (`AGENTS.md`, one-line `CLAUDE.md`) — D2 Move 2, at the Phase 1→2 boundary.

## Phased sequence

Every session: **build one increment → `validate` green → Codex review → Sean merges → stop** ("the pull to keep going is the signal to stop"). Exact per-session increment boundaries are the *implementation-plan* deliverable, not fixed here.

### Phase 0 — Workbench & honesty debt *(no product dependency)*
- **0.1** D2 Move 1: migrate live build rules → `docs/agents/build-sessions.md`; retire charting/wayfinder language from `CLAUDE.md`. + README **Tier 0**: License → Apache-2.0 with the `your-company/` carve-out (#3); fix the "nothing is built" status line. *(Stops live trust-debt in the public README — License currently says "TBD" though #3 locked Apache-2.0.)*
- **0.2** README **Tier 1**: #15 positioning (Sylph + clawcompany; governance lane as the one owned contrast; punch-down line; concede shared ground in the same breath) + #14 prior-art block (bottom, prose bullets, 5-source load-bearing roster, paid-content posture — link don't endorse) + the **3 brief-fact corrections** baked in: Nate's Open Skills is *paid* (open artifact is Open Brain/OB1); Aakash's Team OS isn't fully closed (public starter repo; Hannah Stulberg is the DoorDash origin); the "5 structural tests" is likely his **7-row control map**.

### Phase 1 — Vertical slice / walking skeleton *(People/HR → onboarding-orchestration)*
- **1.1** `scripts/validate.py` skeleton + hand-rolled frontmatter reader (#11: flat `---`/`key: value`/`- list`, raw strings no coercion, ERROR on unsupported syntax) + zero-dep self-check + structure / referential-integrity / secrets-floor (global, high-signal regexes #16) / context-budget (#13: bytes measured, tokens reported; WARN ~20K, ERROR ~50K; AGENTS.md chain >32 KiB hard ERROR) checks, run against a tiny stub fixture.
- **1.2** Ontology schema files (#5 two-tier: name+Direction for every activity; deep fields only for acted-on, forked on Motion) + People/HR executive view + the onboarding-orchestration deep record (automate-path → common core + Substrate + Shape + all-8 Describability Gate) + #5 machinery-follows checks (ERROR when a field backs a running agent, WARN on incomplete thinking, silent on untouched worksheets).
- **1.3** Work-package spec (§6: SKILL.md + harness requirements + compatibility notes + Memory row + owner) + Owner's Card template (#6: 12 spine / 3 track-2 / 2 optional; generator refuses owner/forbidden-action/death-condition) + the worked onboarding skill + its filled card + action taxonomy (read-only / reversible-write / external-side-effect / high-risk) + the fixed Claude-Code hook set (#8, high-risk hard-block, degrades to review-gate prose on other harnesses per #19) + #6 checks (strictness at provisioning; staleness warns).
- **1.4** Org-memory schema (#7: six field groups; provenance/owner/valid_at/source always required; ERROR on missing spine / broken supersession / unsourced `confirmed`; WARN on missing/overdue review_by; body+valid_at frozen, governance fields mutable) + one baseline record (the pre-provisioning baseline #3-gate) + memory-scoped `--diff` mode.
- **1.5** Governance: constitution templates + one compiled rule (#8: typed as one of four objects/four owners, placed on the five-rung ladder, sunset date; ERROR on no-rung-six safety invariant + orphan-prohibition + missing owner at provisioning) + the cross-cutting infra the slice forces: version pin file (#21: `schema_version` + `generated_by_commit`, root-level, independent of `interview/`), proposal schema (#17: diff/reason/evidence/blast-radius), consent-gate blast-radius `--diff` tripwire (#18: escalating change must trace to an approved proposal whose declared blast-radius matches the actual diff), demo canon file + synthetic-identifier check (#16: `demo/`-only ERROR, allowlist to canon or RFC-reserved fiction namespaces). *(Dense — expected to split across sessions in the implementation plan.)*
- **→ D2 Move 2 (Phase 1→2 boundary):** structure is now real → author product `AGENTS.md` (navigation + interview entry, GitHub authoring standard; honestly marks `interview/` and full `demo/` as in-progress) collapse `CLAUDE.md` to the one-line `@AGENTS.md` import, and add the `.cursor/rules/*.mdc` pointers back to `AGENTS.md` (§6 root-file set). `docs/agents/build-sessions.md` continues to carry workbench rules independently.

### Phase 2 — Horizontal fill *(repetition of the proven slice)*
- **2.1** Remaining 7 executive-view ontologies (#5 exec tier: sales, CS, marketing, product, engineering, finance, legal — People/HR done in Phase 1).
- **2.2** Remaining 2 demo deep functions — CS renewal-prep, PM feature-request-triage — as worked slices.
- **2.3** Complete `demo/`: pre-installed synthetic ~20-person B2B SaaS + the 15-minute 3-query script (decision lookup → cross-function synthesis → skill invocation, with the rung-5 governance block firing at the end per #4) + demo canon completeness.

### Phase 3 — The generator (`interview/`)
- **3.1** Consultant protocol (§4): define-the-role-first, one-question-at-a-time (no generation until understanding complete), the evidence-based read-what-exists option, checkpoint approvals, resumable state per #9 (`interview/` folder: fixed-size `00-manifest.md` pointer + one frozen committed file per confirmed layer + a single dirty `_working.md`; confirmed-vs-provisional encoded as git structure, not agent-honored labels).
- **3.2** The generator writes `your-company/` in the demo-proven shape (the demo is the reference target); two-repo semantics (#10: public groundwork clone stays a pull-only engine; the company OS lives in a separate private repo created as the interview's first act; validator runs from the engine clone against it).
- **3.3** Question skeleton (intent-engineering 9-section adaptation — objective/outcomes/health-metrics/constraints/autonomy/edge-cases/stop-rules) + constitution-compiler five-question worksheet (name ritual → name scarcity → is it still real → rewrite as verifiable rule → decide machinery).

### Phase 4 — Finish-artifact bar & provisioning
- **4.1** `delivery/`: org-skills zip provisioning guide (V2 Cowork plugin/marketplace path *documented as V2*, not built).
- **4.2** README **Tier 2**: capability claims turn on — "Not technical? Point your agent at this repo" first-section move, demo walkthrough, `validate` usage.
- **4.3** `docs/`: Known Limitations (synthetic ceiling #16, commit-bit teeth limit #18, ~70% skill auto-invocation), security/privacy section, versioned roadmap, `LICENSE` file (Apache-2.0, #3).

## Scope boundaries (what this build does NOT include)

Per the map's "Not yet specified" (V2/V3 fog) and "Out of scope":
- **V2, documented-not-built:** Classify→Consent→Enforce compliance pack; Cowork plugin packaging / GitHub-synced marketplace walkthrough; morning-Slack-briefing pattern; truth-layer schema; runnable learning-loop skills (session-to-skill extraction, improvement-proposal skill); runnable rung-3 reminder generation; governed *autonomous* application; cross-harness runtime-enforcement parity.
- **V3:** re-interview / drift flow; per-function deepening; adoption scoreboard as coordination-tax-removed; evals/traces recipe.
- **Never (brief §7):** hosted anything, dashboards, per-seat features, an agent runtime, a memory/retrieval engine. groundwork is files — conventions + validator checks only.
- **One worked runnable exemplar** ships in the demo per #8 (the meeting-challenger, compatibility-noted, copy-me reference — *not* generated) to make the V2 path concrete without building V2.

## Build log (decisions & learnings after the design landed)

- **2026-07-22 — Phase 0 + validator foundation landed** (`build/v1-sequence-plan`, green + Codex-reviewed). Tasks 1–7 of the first plan complete: workbench migration, README Tiers 0/1, and `scripts/validate.py` (frontmatter reader, secrets floor, context-budget, referential integrity), 19 `unittest` tests, gate exits 0.
- **Gate-hygiene decision (Option A, locked):** the repo gate keeps `SKIP_RELPATHS={tests, docs/superpowers}` (the scanner's own fixtures/specs necessarily quote example secret & link patterns) and respects `.gitignore` (so `.env`-style files aren't scanned). #16 "global secrets" is read as "all **content** trees"; Gitleaks is the documented backstop for the skipped harness trees; the limitation is recorded in `docs/known-limitations.md`. The cleaner "gate on git-tracked content" model is noted as a V1.5 hardening.
- **Honesty carryover:** the README headline was reframed from a shipped-fact present tense to explicit design-intent (the interview + generator are Phase 3) — Slice 1.2, Task 2.
- **Primary-artifact shape (Slice 1.2):** ontology = one dir per function; deep records are flat-frontmatter one-activity files (so the #11 reader checks them, per #5's "strict where a field backs machinery"); Direction lives canonically in the exec-view table, not duplicated on deep records. Load-bearing — every later slice + the generator inherit it.
- **2026-07-23 — Slice 1.2 landed + merged to `main`** (`e5eebc2`, `--no-ff`, pushed). Ontology schema + #5 checks; 36 tests green. Codex found two real bugs in the plan's check code (list-valued enum crash; blank required field slipping through — both from the #11 reader returning `[]` for a bare `key:`) — fixed in-session with regression tests. The walking skeleton surfaced the schema-translation bugs cheaply on one slice, as intended.
- **Slice-1.3 scope refinement:** the **fixed Claude-Code hook set (#8)** — bundled into 1.3 in the phased sequence below — is **moved to Slice 1.5** to sit with the constitution. It is a global, hand-authored artifact (enforces the taxonomy generically), not part of the per-function vertical slice; 1.3 stays focused on the work-package + Owner's Card + #6 checks layer.
- **Work-package/card shape (Slice 1.3):** a skill is a directory `skills/<name>/` = `SKILL.md` (frontmatter `action_class`/`provisioned`/`ontology`) + `owner-card.md`; provisioning single-homed on `SKILL.md`, card strictness follows it; #6 adds required-spine, track-2-trio, freshness, and three drift checks (card↔skill action_class, card↔ontology owner + source_of_truth). Onboarding classified `external-side-effect`. The `.agents/skills/` + symlink runtime layout (#19) is documented, demonstrated later in `demo/`.
- **2026-07-22 — Slice 1.2 landed** (`build/slice-1.2-ontology`, green + Codex-reviewed). Tasks 1–6 complete: `.gitignore`-awareness + `docs/known-limitations.md`, README headline reframe, ontology vocabularies + exec-table parser, #5 deep-record and ontology checks wired into the gate, People/HR executive view + onboarding-orchestration deep record. 36 tests, gate exits 0. Codex review (HOLD) confirmed two High bugs in the planned check code — list-valued frontmatter enums raised `TypeError`; blank free-text fields (`accountable_owner:`) parsed as `[]` and passed — plus `check_ontology` bypassing the new ignore set; all three fixed in-session with regressions. Deferred (maintainer's call, none block the slice): exec-view header/empty-cell strictness, non-regular-file/decoding safeguards on ontology reads, escaped-pipe table cells, and link-target normalization beyond basename for the orphan check.

- **2026-07-23 — Slice 1.3 landed + merged to `main`** (`build/slice-1.3-skills`, `--no-ff`, green + Codex-reviewed). Tasks 1–4 complete: work-package convention doc, #6 card checks (required spine, track-2 trio, freshness WARNs), the three drift checks wired into the gate, and the worked onboarding-orchestration package + filled card. 76 tests, gate exits 0. Two in-session fixes: the plan's `AUTOMATE_OK` fixture drifted from the real ontology's `gate_source_of_truth` (caught by the new drift check itself); Codex found `provisioned` failing open on malformed values — now a strict yes/no, plus non-scalar/date/ref-containment hardening with regressions. **Maintainer-accepted deviation:** the exemplar ships `provisioned: no` (Codex honesty fix — the skill isn't live and the Slice 1.4 baseline it reads doesn't exist; the false runtime-hook claim in the compatibility notes was removed with it). The strict at-provisioning ERROR path is exercised by tests; real provisioned content arrives with the demo (Phase 2.3).

- **Org-memory shape (Slice 1.4):** one record per `.md` file under a `memory/` dir; flat-frontmatter #7 fields; `check_memory` is stateless and record-level (nothing silent — no untouched-worksheet state); index lists live records only (live-not-in-index WARN, superseded silent). Exemplar memory lives at root `memory/`; the validator discovers records under any `memory/` dir so `demo/**/memory/` and `your-company/memory/` are picked up later unchanged.
- **Provisioning gate + the flip (Slice 1.4):** #5 baseline-gates-provisioning is encoded as a `baseline:` path on a provisioned `SKILL.md` that must resolve to a memory record. Slice 1.4 creates the onboarding baseline — the precondition `provisioned: no` was waiting on — so the onboarding skill flips to `provisioned: yes` (recommended), making the walking skeleton walk the #5/#6 strict path on real content and resolving the 1.3 deviation. The flip is an isolated step; dropping it keeps `no`.
- **`--diff` split out (Slice 1.4b):** #7's stateful `--diff <base>` immutability mode is its own micro-slice (git coupling + immutability rules), after 1.4.

- **2026-07-23 — Slice 1.4 landed + merged to `main`** (`build/slice-1.4-memory`, `--no-ff`, green + Codex-reviewed). Tasks 1–5 complete: org-memory schema doc, `check_memory` (#7 record shape + supersession invariants + index cross-check), the #5 provisioning gate on `check_owner_cards`, and the worked baseline record + index. The onboarding skill flipped to `provisioned: yes` citing `baseline: memory/onboarding-baseline.md` — the 1.3 deviation resolved; the walking skeleton now runs the #5/#6/#7 strict path on real content. 109 tests, gate exits 0. Codex review ran three rounds (HOLD → HOLD → MERGE), all crash/fail-open findings fixed in-session with regressions: safe UTF-8 readers for memory files; non-scalar guards on `owner`/`baseline`/`superseded_by`; unparseable `review_by` WARN; and reference containment — `baseline`/`superseded_by` must be repo-relative literals resolving to a non-symlink memory record (absolute, `../`-re-entering, drive-letter/UNC, and symlink aliases all ERROR). Deferred (maintainer's call, index-topology per the scope note): no-index tree emits no per-record WARN; nested `memory/x/memory/` outer-index false positive; memory-dir-as-root discovery skip; POSIX `a:notes/…` drive-guard false rejection (Low).

- **Slice 1.4b planned (`--diff`):** the immutability rules live in a pure `check_memory_diff(old, new, path)` (git-free, exhaustively unit-testable); git is confined to `memory_diff_findings(root, base)` (base-version fetch via `git show`, deletion detection via `git ls-tree`). `main()` grows `--diff <base>`, running the stateless `validate()` **plus** the diff findings. Immutability enforced: frozen body/`valid_at`, provenance forward-only, append-only `source`, set-once supersession fields, no deletion. The diff infrastructure is deliberately reusable — #18's consent-gate blast-radius tripwire (Slice 1.5) diffs escalating changes the same way.

- **2026-07-23 — Slice 1.4b landed + merged to `main`** (`build/slice-1.4b-memory-diff`, `--no-ff`, merge `c310d4f`). Both plan tasks landed; 146 tests, plain run and `--diff main` exit 0. Two plan bugs surfaced by TDD/review and fixed in-session with regressions: the plan's list-prefix `source` check failed its own scalar-append test (append-only now means: earlier entries frozen, the final existing entry may be extended in place, new entries may follow); and the walker-driven git layer had silent-pass holes. Codex review ran four rounds; the big learning is architectural: **the diff scan must be driven by the base file list (`git ls-tree -z`), never the working-tree walker** — otherwise `.gitignore`, root-inside-memory invocations, case-folding renames, or walker skips can exempt a committed record. Fail-closed hardening landed for: unknown base ref, unreadable/undecodable base or working versions, malformed new-side frontmatter (duplicate-key trick), symlinked records or ancestor dirs, case-only renames (exact-name walk), NFC/NFD normalization (`core.precomposeunicode`), newline-containing repo paths (refuses rather than mis-scopes), and CRLF base blobs (no phantom edits). Deferred by charter (documented in known-limitations): check-then-read TOCTOU race — PR-time CI on a quiescent checkout is the supported setting. Pre-existing stateless-walker gap noted for the maintainer: `check_memory` never walks a symlinked memory *directory* (predates this slice).

- **2026-07-23 — Slice 1.4b landed + merged to `main`** (`c310d4f`, `--no-ff`, pushed). The `--diff` memory-immutability mode: pure `check_memory_diff` + git-backed `memory_diff_findings`. 146 tests, gate + `--diff main` exit 0. Codex ran four rounds; the scan is now **base-file-list-driven** (`git ls-tree -z`) so nothing in the working tree can hide a committed record; everything fails closed (unknown ref, non-UTF-8, duplicate-key frontmatter, symlink/case-fold/NFC-NFD/newline-path aliases). `subprocess`+`unicodedata` joined the zero-dep allowlist. Deferred: check-then-read TOCTOU (documented non-goal — PR-time quiescent checkout); the *stateless* walker not entering a symlinked `memory/` dir (pre-existing; the diff layer catches it — candidate for a hardening step in 1.5c).
- **Slice 1.5 decomposition (governance is too big for one increment):** **1.5a** constitution rule schema + #8 typed-rule checks (no-rung-six / orphan / owner-at-provisioning) + one compiled rule; **1.5b** fixed Claude-Code hook set (#8 item 2) + #19 degradation; **1.5c** version pin (#21) + skew checks (`since:` per check) + the stateless-symlink hardening; **1.5d** proposal schema (#17) + consent gate (#18) + blast-radius tripwire (reuses 1.4b `--diff`) + changelog. **#16 demo canon + synthetic-identifier check → moved to Phase 2.3** (demo-scoped check is inert without `demo/`).
- **Constitution rule shape (Slice 1.5a):** one file per rule under `governance/constitution/`; "four objects / four owners" (§5.1) rendered as inline owned fields (`value`/`value_owner`, `runtime_check`/`_owner`, `human_appeal`/`_owner`, plus the rule statement + `owner`); `rung` on the five-rung ladder (no rung six); `sunset`; ritual provenance (`ritual`/`scarcity`/`surviving_job`) for orphan-prohibition. Active = rung-placed (strict); no rung = draft (WARN). No-rung-six enforced as high-risk ⇒ human-appeal present. The worked rule governs the onboarding skill's `proposed-only` access-grant action.
- **2026-07-23 — Slice 1.5a built (`build/slice-1.5a-constitution`, awaiting merge).** `governance/README.md` (compiler convention), `RUNGS` + `check_constitution` wired into `validate()`, and the worked rule `access-grants-need-human-signoff.md` (human-decision rung, governs the onboarding skill's proposed-only access grant). 177 tests, gate exit 0. Codex ran six rounds; fixed in-session with regressions: safety-spine checks (no-rung-six, action_class validity, sunset WARNs) run on **drafts** too — only `owner` + the four-object completeness wait for provisioning; `action_class` is **required** (ERROR active / WARN draft) since it drives no-rung-six; unparseable `sunset` WARNs; scalar-only `owner`/appeal/`reassigned_to` fields; `.gitignore` parity with the other walkers; full four-object + H1-and-substantive-body enforcement at provisioning; and a placeholder policy (`_answered`): none/TBD/etc. — bare, quoted, or markup-formatted — never satisfy a provisioning or safety field, `repeals: none` is an explicit no-repeal, comment-only bodies are not rule statements. Also fixed a plan-fixture bug (bare `replace()` of the owner line mangled `runtime_check_owner`). **Rejected** (contradicts the plan's locked "no silent case at the rule-file level"): round-5 ask to keep untouched worksheet *files* silent. **Deferred** (maintainer's call): requiring `ritual`/`scarcity` provenance on every active rule — a severity the #8 contract doesn't list; further placeholder-token variants are the #8 accepted-risk class.

- **2026-07-23 — Slice 1.5a landed + merged to `main`** (`2226d94`, `--no-ff`). Constitution convention, `RUNGS` + `check_constitution` (#8 typed-rule checks), and the worked access-grants rule. 177 tests, gate exits 0. Codex ran six rounds; the important catch: **the draft branch skipped the safety spine** — a high-risk *draft* with no appeal path passed green. Now no-rung-six, `action_class` validity, and sunset WARNs run on drafts too; only owner + four-object completeness wait for provisioning. Also: `action_class` required (ERROR active / WARN draft) since it drives no-rung-six; placeholder-laundering closed (`TBD`, `**TBD**`, `# TODO`); `repeals: none` reads as explicit no-repeal.
- **Two 1.5a deferrals — decided (maintainer's call, 2026-07-23):** (1) **untouched-worksheet silence stays as planned** — Fable's rejection of Codex's ask is upheld; #8's silence clause is the #5 parallel (no file = silent), so silence is decided by **location**: blank worksheets live in `governance/worksheets/`, which `check_constitution` does not scan. (2) **Active-rule provenance (`ritual`/`scarcity`/`surviving_job`) is a WARN, not an ERROR** — #8's ERROR tier is reserved for safety invariants; provenance is thinking-quality. Both are folded into Slice 1.5b Task 1 rather than left to rot.
- **Hook-set shape (Slice 1.5b):** `governance/hooks/` is a **shipped artifact, not engine configuration** — the gate is not installed into this repo's `.claude/settings.json` (dogfooding groundwork's own machinery is retained map fog). The hook is **Python 3 stdlib** (the upstream docs' `bash`+`jq` example is rejected — groundwork cannot assume `jq`), split pure-`classify()` / thin-IO like 1.4b's git layer. **Live hooks contract fetched from the official docs 2026-07-23** — `hookSpecificOutput.permissionDecision` (`allow`/`deny`/`ask`/`defer`), not the legacy top-level `decision: block`. Unreadable input → `ask`, never a silent allow. `check_hooks` **existence-checks the enforcement claim** (snippet valid JSON; every declared `command` resolves) because a named-but-unwired guard is false safety; the zero-dep self-check extends to all shipped hook scripts.

- **2026-07-23 — Slice 1.5b built (`build/slice-1.5b-hook-set`).** The runnable governance floor: `governance/hooks/` (stdlib-only `action_class_gate.py` with pure `classify()`/`decide()`, quoted-path `settings.snippet.json`, `review-gate.md` #19 degradation, README), `check_hooks` wired into `validate()`, and both 1.5a deferrals closed (provenance WARN; worksheet silence by location — `governance/worksheets/`). 238 tests, gate exit 0, live stdin round-trips verified. **Codex ran eight rounds** (the slice record); every finding was fixed in-session with a regression test except one rejected redesign (position-aware matching — needs a shell parser, opens `sh -c` bypass holes; documented). The big learnings: (1) **exemptions are launder-bait** — the `git clean --dry-run` exemption was bypassed three consecutive rounds (later command's `-n`, comment text, `--exclude=--dry-run`) and was removed; a denied dry run fails safe, a laundered force-clean does not. (2) **The registration is part of the enforcement claim** — `check_hooks` now requires a non-symlinked, redirect-free PreToolUse/Bash command hook targeting `action_class_gate.py` itself; anything less certifies a guard that cannot fire. (3) A regex floor's false-negative supply is unbounded; the honest stopping rule is the artifact's documented posture (high-signal floor, deny-leaning), not review silence. `shlex` joined the zero-dep allowlist (stdlib; quoted hook paths).

- **2026-07-23 — Slice 1.5b landed + merged to `main`** (`035fa59`, `--no-ff`). The fixed Claude-Code action-class gate (`governance/hooks/action_class_gate.py`, stdlib-only, pure `classify()` + thin IO), the registration snippet, the #19 `review-gate.md` degradation, and `check_hooks` (existence-checks the enforcement claim: valid JSON, non-symlinked hook targeting the gate script, `review-gate.md` present). Both 1.5a deferrals closed. 238 tests, gate exits 0. Codex ran **eight rounds** (repo record); stopping rule: a regex floor's false-negative supply is unbounded, so the honest line is the artifact's documented posture (high-signal floor, not a sandbox), not review silence. A dry-run `git clean -n -f` denies — a false positive that fails safe onto the human path (documented).
- **#21 scope decision (maintainer, 2026-07-24):** **build the kernel + gate logic, defer the per-check `since:` retrofit.** Rationale surfaced: at `SCHEMA_VERSION=1` ~90% of #21 is dormant (no v2 exists; the `since:` demotion can never fire — all checks are `since: 1`, all content pins at `1`), and the pin is company-instance metadata (real content lands with `demo/`). Slice **1.5c** ships `SCHEMA_VERSION` + the `groundwork.pin` format + `MIGRATIONS.md` contract + `check_version_pin` (skew 0 safe / skew ≥ 1 one migration ERROR / reverse-skew WARN, fixture-proven, activates on `demo/`), so the generator has the pin contract; the `since:` tags + scatter-suppression are documented in `MIGRATIONS.md` and wired at the first real v2. 1.5c also folds in the deferred **stateless-walker symlinked-directory hardening** (make the silent skip a loud WARN).

- **2026-07-26 — Slice 1.5c built (`build/slice-1.5c-version-pin`, awaiting merge).** The #21 kernel: `SCHEMA_VERSION = 1`, `check_version_pin` (skew 0 silent / skew ≥ 1 exactly one migration ERROR referencing `MIGRATIONS.md` / reverse-skew WARN "pull the engine"; missing `schema_version` ERROR; non-scalar/non-integer ERROR; missing `generated_by_commit` WARN), `MIGRATIONS.md` (pull promise + migration contract + pin format + the deferred-`since:` note), and the folded-in 1.4b deferral `check_symlinked_dirs` (symlinked content dir → loud WARN, skip-set parity with `iter_files`). 254 tests, gate exits 0; the engine repo stays pin-less by design (fixture-proven; activates on `demo/`/`your-company/`). One builder deviation with regressions: the pin is read via the existing `_load_frontmatter` safe reader, not the plan's bare `open()` — a non-UTF-8 pin ERRORs instead of crashing (the recurring Codex crash-path class). Codex ran two rounds, verdict MERGE both; all findings were LOW test-coverage gaps, every one closed in-session (exact skew cardinality/silence, malformed-pin fail-closed set — empty/unclosed/duplicate-key, symlink skip-set parity incl. `SKIP_DIRS`, `validate()` integration).

- **2026-07-26 — Slice 1.5c landed + merged to `main`** (`7b2dbe2`, `--no-ff`). `SCHEMA_VERSION=1`, `check_version_pin` (pull promise implemented exactly), `MIGRATIONS.md`, and the symlinked-directory hardening. 254 tests, gate exits 0. Codex: two rounds, MERGE both — only LOW test-coverage gaps, all closed in-session; one deviation (pin read via the safe `_load_frontmatter` reader, not a bare `open()`, so a non-UTF-8 pin fails closed). Deferred per scope: `since:` retrofit + scatter-suppression (documented in `MIGRATIONS.md`).
- **Slice 1.5d decomposition (the headline — governed self-improvement):** **1.5d-i** proposal-file schema (#17/#18: domain, blast-radius declaration, pending-only lifecycle) + governance changelog + their static checks; **1.5d-ii** the blast-radius `--diff` **tripwire** (declared-vs-actual matching, reusing 1.4b's git infra) + changelog append-only enforcement + the documented "commit bit is the real teeth" Known Limitation. Schema-before-enforcement, as every slice.
- **Proposal/changelog shape (Slice 1.5d-i):** a proposal is one pending-only file in `proposals/` (frontmatter `target`/`blast_radius` (`track1-body`|`escalating`)/`reason`/`evidence`/`status: pending`); `check_proposals` validates the static schema + routing domain (skills+rules only) + rules-never-`track1-body` + pending-only lifecycle (incomplete → WARN "demote to a working note"). The changelog is `governance/changelog.md`, append-only index of auto-applied track-1 changes. **No committed live proposal** (transient artifact, like the #21 pin) — proven by fixtures, activates when a real proposal exists. Diff-based teeth deferred to 1.5d-ii.

- **2026-07-26 — Slice 1.5d-i built (`build/slice-1.5d-i-proposals`, awaiting merge).** The two records of governed self-improvement: `proposals/README.md` (consent-gate convention: schema, three-bucket routing, consent ladder), `governance/changelog.md` (append-only auto-apply index, header only), and `BLAST_RADIUS` + `check_proposals` + `check_changelog` wired into `validate()`. 281 tests, gate exits 0; no committed live proposal by design (fixture-proven, like the #21 pin). **Codex ran five rounds (HOLD ×4 → MERGE)**; every finding fixed in-session with regressions: (r1) lexical path-alias laundering (`skills/../governance/constitution/…` dodging rules-never-auto-apply) + non-scalar `status` fail-open + blank/aliased changelog fields; (r2) the same class at the *filesystem* level — classification now by realpath containment (1.4b precedent), a symlink named SKILL.md pointing at a rule fails closed — plus `track1-body` requires a `SKILL.md` target (an Owner's Card target is the same contradiction ERROR), empty-body and list-valued-`reason` incompleteness WARNs; (r3) multiline HTML comments are not a substantive body; (r4) static diff-presence (`## Diff` content) joins #17 completeness; (r5) unreadable `proposals/` dir ERRORs fail-closed instead of crashing. The through-line: **alias laundering must be closed at both the lexical (normpath) and filesystem (realpath) layers** — one without the other is a bypass. Next: 1.5d-ii (the `--diff` tripwire + append-only enforcement).

- **2026-07-26 — Slice 1.5d-i landed + merged to `main`** (`18c4ffc`, `--no-ff`). `proposals/README.md`, `governance/changelog.md`, and `BLAST_RADIUS` + `check_proposals` + `check_changelog` wired into `validate()`. 281 tests, gate exits 0; no committed live proposal by design. Codex ran five rounds (HOLD ×4 → MERGE), every finding fixed in-session with regressions.

- **Tripwire shape (Slice 1.5d-ii) — three load-bearing calls, pre-made from the record and flagged for the maintainer:**
  1. **The tripwire is scoped to *governed roots* — a directory carrying a #21 `groundwork.pin` — so it does NOT fire on the engine repo.** Three locked record points determine this together: 1.5d-i's "no committed live proposal" (a proposal is transient), 1.5c's "the pin is company-instance and the engine stays pin-less", and 1.5b's "whether groundwork dogfoods its own governance machinery on this repo is retained map fog, not V1". Firing on the engine root contradicts all three at once (the exemplar skill is `external-side-effect`, so *every* edit to it would escalate and demand a committed proposal that never applies). The dead-path risk is answered by a **pure, exhaustively unit-tested classification core** plus CLI tests against real temp git repos that carry a pin; it stops being dormant at **Phase 2.3**, where `demo/`'s pin can carry a real pending proposal in the rung-5 governance block.
  2. **A missing changelog line for a track-1 body change is a WARN, not an ERROR.** A stateless validator cannot distinguish an agent's auto-apply from the maintainer's own hand-edit; ERRORing would false-positive on ordinary work and fill the ledger with non-auto-applies, destroying the one-glance property #17 gives as the justification for conceding pre-approval. The harm class is accountability, not safety — the same principle already applied to active-rule provenance in 1.5a. The ERROR stays exactly where #18 puts it: **no-proposal** and **declared-vs-actual mismatch**.
  3. **A deleted governed file is a WARN.** `check_proposals` requires a proposal `target` to be an existing file, so a deletion can never be traced to one — an ERROR would be an unsatisfiable gate. Retirement is legitimate (`sunset` / `retirement_condition`); its honest record is the consent commit. (Memory records keep their ERROR — different doctrine: superseded, never deleted.)
  
  Architecture: a **pure git-free core** (`_governed_class`, `classify_governed_change`, `_changelog_append_only`) behind a thin git layer, with 1.4b's plumbing **extracted once** into `_git_diff_context`/`_git_show` and shared by both `--diff` modes. The candidate set is **base `ls-tree` ∪ working-tree walk** (memory only needed base-driven; governance must also see *additions*, and `git diff` never lists untracked files). Governed roots are collected from base **and** working tree so deleting the pin cannot un-govern the diff. Every uncertain classification resolves to `escalating` — a misread must never license an auto-apply.

- **2026-07-26 — Slice 1.5d-ii landed + merged to `main`** (`acb40af`, `--no-ff`). The #18 tripwire: `_git_diff_context`/`_git_show` extracted and shared by both `--diff` modes, the pure classification core, and `blast_radius_diff_findings`. 341 tests (1 designed skip — a case-sensitive-filesystem test on macOS), gate + `--diff` exit 0. **Codex ran four rounds, converging at 0 findings.** One plan bug fixed in-session: the classifier escalated *unchanged* rules (the candidate set is the whole base tree) — fixed with a no-op short-circuit plus regressions. The three review rounds each broke the previous round's fix, and the through-line is **scope laundering**: (1) **nested-pin shadowing** — a `groundwork.pin` planted inside a package could reshape the inner path and un-govern or *downgrade* a rule, so enforcement now runs under **every** containing governed root, not just the nearest; (2) **case/Unicode laundering** — `Governance/` renames, `straße`→`STRASSE` folds, and NFC-vs-NFD splits could walk a path out of its root, so containment now matches path *components* under NFC+casefold with exact-case precedence; (3) **unreadable directories** read as "missing" (a deletion WARN) or were silently pruned by `os.walk` — both now fail closed. The lesson generalizes past this slice: **when a check's scope is derived from repo content, the scope derivation is itself an attack surface** — every laundering round targeted the scope, never the classification. **Phase 1 governance is complete.** Four findings deferred to the maintainer (recorded in the `validator-deferred-findings` memory); none is a fail-open on the ERROR path.

- **Move 2 decisions (D2, Phase 1→2 boundary) — three calls, pre-made and flagged:**
  1. **`CLAUDE.md` collapses to a literal one line (`@AGENTS.md`), so the build rules stop auto-loading.** D2 and the success criteria both say "one-line"; Claude Code's own docs show exactly that file content; and the engine should model the shape adopters copy. The mitigation was already in place — the maintainer's agent memory carries `explain-before-deciding` with the note that it *survives CLAUDE.md replacement*. Residual risk: a session that does not start from a kickoff prompt loses the workbench rules from context, so transport is now the orchestrator's job. `AGENTS.md` carries a "Working on groundwork itself" section linking `docs/agents/build-sessions.md` — one hop from always-loaded context, zero workbench prose in the adopter-facing surface.
  2. **The per-file context-budget check is retired; #13's thresholds move to the always-loaded aggregate.** `check_context_budget` had been running the 20K/50K thresholds over *every file in the repo*, which is why `scripts/validate.py` began WARNing about its own size — the validator flagging a Python script that never enters an agent's context. #13 specifies those thresholds for the *always-loaded surface* (root instructions + the AGENTS.md/CLAUDE.md chain + always-on rules + Σ skill descriptions), an aggregate. Honest cost, accepted: a huge `SKILL.md` **body** no longer ERRORs — correct per #13 (bodies load only on invocation) but a real loss of a crude size ceiling, documented in `docs/known-limitations.md`.
  3. **`AGENTS.md` is honest that the interview does not exist.** D2 calls it the "interview entry", but `interview/` is Phase 3 and `demo/` is Phase 2.3, so the file states plainly what is built and what is not. D3's tiering applied to the front door.
  
  **Live-contract verification (wwf5d §1.3, and it changed the code):** Claude Code, Codex, Cursor, and the AGENTS.md spec were all fetched 2026-07-27. Confirmed: Claude Code reads `CLAUDE.md` and **not** `AGENTS.md` (the `@AGENTS.md` import — or a symlink — is the documented bridge); imports allow relative/absolute paths, max **4 hops**, and parsing **skips code spans and fenced blocks**; imports do *not* reduce context, so the aggregate must follow them. Codex "concatenates files from the root down, joining them with blank lines" and "stops adding files once the combined size reaches `project_doc_max_bytes` (32 KiB by default)" — silent truncation, so the chain check is an **ERROR**, not a WARN. **Two facts new since the #13 research:** Codex checks `AGENTS.override.md` **before** `AGENTS.md` at every level (the chain model must honor the override precedence), and `project_doc_fallback_filenames` lets an adopter add instruction filenames the repo cannot know about (noted, not implemented). Cursor requires the **`.mdc`** extension — plain `.md` in `.cursor/rules/` is ignored.
  
  **The seam this slice must not trip on:** `iter_files` skips every dot-directory, so `.claude/rules/` and `.cursor/rules/` are invisible to the walker. Any rules-directory check built on it would scan zero files and pass vacuously — the corpus-void trap. Both directories are opened by path, with tests that prove a real rule file is measured.

- **2026-07-27 — D2 Move 2 landed + merged to `main`** (`8a0956d`, `--no-ff`). `AGENTS.md` (124 lines, honest built/not-built status), the one-line `CLAUDE.md`, `.cursor/rules/groundwork.mdc`, and three checks: `check_agents_chain` (Codex's 32 KiB silent-truncation cap, with `AGENTS.override.md` precedence), `check_always_loaded_budget` (#13's thresholds moved from per-file to the aggregate — the `scripts/validate.py` false positive is gone), `check_root_files` (root-file drift ERROR; missing Cursor pointer WARN). 378 tests, gate exit 0, WARN count 10 → 7. **Codex ran three rounds**; five round-1 findings were plan-code contract bugs, each fixed with regressions: fail-open sizing of a stat-able-but-unreadable instruction file; an import-depth off-by-one against Claude Code's 4-hop rule; the 1,536 description cap measured in **bytes** instead of **characters** (undercounting multibyte descriptions); the aggregate discarding read/parse findings from dot-directory rules; and a vacuous test. Round 2's catch is the one worth carrying: **depth-first traversal with a seen-set can lock a shared import in at a deep first visit and undercount its children** — expansion is now breadth-first, with a diamond-import regression. The generalizable lesson: *when a traversal both dedupes and depth-limits, visit order becomes part of the contract.*

- **Slice 2.1 decisions — two deferral sets folded in (maintainer asked, 2026-07-27):**
  1. **`_strip_code`'s fence handling is a fail-open, not a nit — fold it in.** The regex knows only ` ``` ` fences, but a CommonMark **tilde** fence is also a fenced code block that Claude Code's import parser skips. So a `CLAUDE.md` that merely *documents* `@AGENTS.md` inside a `~~~` block **satisfies the drift check while Claude Code imports nothing** — silent root-file drift passing an ERROR-level guarantee. Replaced with a line-based scanner. **The tension, decided:** the two consumers want opposite safe directions — for the drift check over-stripping is safe (false ERROR, loud) and under-stripping is fail-open; for the budget it inverts. **Tiebreak: over-strip**, because the drift guarantee is ERROR-level and the budget is a WARN threshold. Accepted cost: an escaped backtick reads as opening a span (documented).
  2. **The aggregate counts `AGENTS.md` twice — a real defect, folded in.** Once as the root instruction file, once as `CLAUDE.md`'s import. Conservative in direction but wrong in model: no harness loads it twice (Codex reads `AGENTS.md`; Claude Code reads `CLAUDE.md` → `AGENTS.md`), so the honest model is **max across harnesses**, not union-with-duplicates. It matters because the upper threshold is an **ERROR that fails the gate** — double-counting can block a repo for budget it does not spend. Deduped by realpath.
  3. **Absolute in-repo imports: keep the ERROR, fix the message.** An absolute path committed in a repo resolves only on the machine that wrote it, so failing is right — but calling it "drift" misdiagnoses it.
  4. **The Slice 1.2 exec-table deferrals are folded in *because this slice multiplies their blast radius by eight*.** A misspelled `Direction` header makes `parse_exec_table` return `[]`, which today means every Direction in that file goes unchecked **with a green gate**. Deferred when one function existed; closed now that there are eight. Also: empty Activity cells, escaped pipes, non-regular `.md` entries (a directory named `x.md` crashes the read), and deep-record listing matched by **realpath** instead of basename (which also fixes the `#fragment` false WARN).
  
  **The through-line worth keeping:** a deferred fail-open is priced by how much content it governs. These two sets sat harmlessly at one function and one root file; the moment the slice takes ontologies to eight functions, the silently-unparsed table converts from theoretical to likely. *Fold a deferral in at the slice that scales it, not at the slice that is nearest.*

- **Ontology exemplar framing (Slice 2.1):** root `ontologies/` ships an opinionated Direction for every activity, which is useful as a default map and false as an assertion about a specific company. The locked "engine repo ≠ generated company" convention already covers it, but nothing in the *files* said so. Every executive view now carries a one-line "starting template, not a claim about your company" frame, with the tier rules and the depth doctrine in a new `ontologies/README.md`; `people-hr/` is retrofitted so the eight read as one system.

- **2026-07-27 — Slice 2.1 landed + merged to `main`** (`5fc61c6`, `--no-ff`, 35 commits). All 7 remaining executive views (8 functions × 10 activities, zero new findings), `ontologies/README.md`, the template-frame retrofit, and both folded deferral sets. 492 tests (up from 378), gate + `--diff` exit 0, the 7 pre-existing WARNs unchanged. **Codex ran 32 rounds with ~25 fix commits** — the repo record by a wide margin. Two arcs: (1) the §6 drift check was made **structural rather than parser-dependent** — only a standalone `@AGENTS.md` as the first content line satisfies it — and Codex verified that against the installed Claude Code binary; (2) the exec-table parser survived 8 hardening rounds (decoy and shadowed tables, fenced and comment-wrapped examples, GFM no-leading-pipe and blockquoted tables, rendered-cell semantics, image-vs-link, delimiter presence) and round 32 still found six more divergences, with a **FIX-BEFORE-SHIP** verdict the maintainer merged over. Also logged: the Codex runtime crashed mid-review four times, needing manual stale-job cancellation.

- **The 32-round diagnosis — and the fix (Slice 2.2a):** the treadmill has a root cause, and it is not parser quality. **There are two kinds of parser in this repo, and the exec table was built as the wrong one.** The 1.5b hook regex reads *adversary-supplied shell* — input groundwork does not control — so its false-negative supply is unbounded and the honest stopping rule is "the artifact's documented posture, not review silence." The #11 frontmatter reader reads *a file groundwork's own generator writes* — input groundwork controls — so its rule is a restricted grammar where "any other syntax ERRORs." The executive-view table is squarely the second kind and was implemented like the first, which is why rounds 24–32 kept finding GFM divergences and why round 33 would find more: GFM is a large spec and each round only closes the case it found. **Resolution: canonical-form pinning** — one legal table shape, ERROR on everything else. This is not a new doctrine but #11's locked doctrine applied to the second structured surface, making the two structured readers consistent instead of adding a third philosophy. The round-32 list and every earlier class stop being *handled* and become *unreachable*; ~150 lines retire. **Accepted cost:** benign formatting variance (alignment colons, indentation, column reordering) becomes a gate failure — the same trade #11 already accepted, mitigated by error messages that name the required shape and by `ontologies/README.md` showing it. **Method note:** the 32 rounds of adversarial inputs are **converted, not discarded** — each is kept as a *rejection* test, which is strictly stronger than asserting a particular parse, and the slice fails if the test count drops.

- **First track-1 skill (Slice 2.2a):** onboarding is `external-side-effect`, so every governance path the repo exercises is the escalating one — the auto-apply boundary, the changelog requirement, and the `track1-body` branch of the #18 tripwire have only ever run against fixtures. Scoping renewal-prep as *assemble the brief; do not send it or touch the contract record* makes it genuinely `reversible-write` and gives that half of the governance model real content. The boundary is stated in the skill body and the card's `forbidden_actions`, not just in the frontmatter, because the classification is only as honest as the scoping.

- **2.2 split into 2.2a / 2.2b:** the runway lists CS renewal-prep and PM feature-request-triage as one item, but a single function's vertical took three slices in Phase 1 (1.2 ontology, 1.3 skill + card, 1.4 baseline + provisioning). The schema is proven so this is content rather than discovery — but two full verticals plus a parser replacement in one session is how a slice becomes another 32-round evening. One vertical per slice.

- **2026-07-27 — Slice 2.2a landed + merged to `main`** (`36a236c`, `--no-ff`). The canonical exec-view grammar (net **−79 lines** in `validate.py` despite gaining stricter rules) plus the CS renewal-prep vertical — the repo's first **track-1** (`reversible-write`) provisioned skill. 524 tests (492 → 524), gate + `--diff` exit 0, 7 WARNs unchanged. The corpus survived intact: all 8 views parse at 10 activities / 0 findings, no view needed loosening, and no adversarial test was deleted (`TestExecTableHardening` 29 → 30, all inputs verbatim and renamed to say what they reject; `TestCanonicalExecTable` +19). **The canonical grammar was validated by the review it then survived:** Codex returned `needs changes` on six findings, all real, and the through-line is worth keeping — **findings 2–4 were one mistake, not three: the plan's regexes were taken verbatim while a *stricter* README was written around them.** A grammar that exists to be exact, is documented as exact, and is quietly permissive is the worst possible version of this design. The re-review then caught the opposite error — the fix banned `[`, `]`, `_` as *characters*, rejecting legitimate names like `Coverage [EMEA]` and `SOC_2 compliance`, when the promise was to ban the *syntax*. **Both directions of that pair are the real lesson: a restricted grammar has two failure modes, and tightening is as reviewable as loosening.**

- **Schema-version decision (maintainer, 2026-07-27):** the grammar tightening makes previously-valid content newly ERROR under an unchanged `SCHEMA_VERSION = 1`, which by MIGRATIONS.md's own words ("same schema version → pull is always safe") is a breaking change. **Resolved: stay at v1, and record why** — `groundwork.pin` lands with `demo/` in Phase 2.3, so no pinned content exists anywhere and the breakage is unreachable. The reasoning now lives in `MIGRATIONS.md` **with the line it does not cross**: once any company repo carries a pin, a tightening like this is a v2 change and gets a migration note no matter how small the syntax change is. Bounding the exception is what stops it becoming precedent — "no adopters yet" is exactly the excuse that erodes a versioning contract.

- **Process note (2026-07-27) — a self-fixed blocker shipped unreviewed.** Codex's re-review returned `DO NOT MERGE` on two findings; both were fixed and the slice merged without a third pass, so `cfd3bcc` (the syntax-vs-character rule) carries self-verification only. Planning-side probe of the merged code: the rule **holds** for inline links, images, and emphasis, and correctly accepts `Coverage [EMEA]` / `SOC_2 compliance` — but **reference-style link syntax leaks** (`[x][r]` and `[x]` parse as plain text, and render as links whenever a `[r]: url` definition sits elsewhere in the file, which is legal today because a definition line carries no `|`). Low severity — nothing reads the Activity string as a link — but it is a documented restriction that does not hold, in the one grammar whose value is being documented-exact. Folded into 2.2b Task 1, fixed by **constraining the file** (no link reference definitions) rather than enumerating bracket spellings, so `Coverage [EMEA]` stays legal *and becomes provably literal*. **The standing question for the maintainer:** whether a blocker the builder fixes itself must be re-reviewed before merge — the gate's value is in the *fix* being reviewed, not only the finding being found.

- **Motion-pivot proof (Slice 2.2b):** onboarding, renewal-prep, and feature-request-triage are all `Motion: automate`, so every acted-on activity the repo has ever had sits on the automation path and #5's **central pivot** — Substrate, Shape, and the eight Gate fields are required *only* for `automate`/`build`; buy/hire/wait stay cheap at ~4 answers — has only ever been exercised by fixtures. One ~15-line record (engineering → technical hiring loops, `Direction: up`, `Motion: hire`) probes it on real content, and its verification step is written to distinguish "the check was silent" from "the check never ran." It also lets the ontology say something it currently cannot: **a deliberate decision not to build an agent.** An ontology that only ever records automation verdicts reads as an automation funnel.

- **2026-07-28 — Slice 2.2b landed + merged to `main`** (`d90f5d5`, pushed). The product feature-request-triage vertical, the `]:` link-reference-definition ban closing the bracket class, and the `Motion: hire` record. 535 tests, gate + `--diff` exit 0. **Codex: two rounds, ending SAFE TO MERGE**, confirming the class closed (every container, wrapped-label, and deferred-destination definition form caught; no reachable false negative) and the over-tightness accepted as documented and fail-closed. Two results worth keeping: (1) **the Motion pivot works, first time on real content** — `check_deep_record` demanded nothing of the `hire` record, so no validator fix was needed and Task 5 stayed content-only; (2) **the plan's own Task 1 regex had a real hole** — it was line-anchored and missed blockquoted, list-item, and label-wrapped definitions — and the builder sent the fix back through the same review thread instead of shipping it self-verified. That is the Design-call-1 pattern running at full strength, and it is the live data point for the standing re-review rule.

- **Phase 2.3 decomposition (`demo/` is the largest remaining item) — and the finding that reorders it.** Five structural checks are **root-only**: `check_ontology`, `check_owner_cards`, `check_constitution`, `check_proposals`, `check_changelog` all start at `root/<content-dir>`. Only `check_memory` walks. So a complete synthetic company under `demo/` — ontologies, skills, cards, rules, proposals — would be scanned by **nothing**, with a green gate throughout: authoring the demo would *look* like proof the schema works and prove nothing. That is the corpus-void trap at phase scale, and it forces instance-awareness to land **before** any demo content. Sequence: **2.3a** instance-aware validation (fixture-proven, no demo content; also closes the 1.5d-ii nested-root deferral); **2.3b** demo canon (#16) + `check_synthetic_identifiers` + the demo skeleton; **2.3c** demo ontologies + org memory (incl. the async-standups supersession record, demo query 1); **2.3d** demo skills + Owner's Cards + constitution incl. the rung-5 human-decision rule, plus the meeting-challenger exemplar (#8 item 3); **2.3e** the 15-minute 3-query script + `demo/groundwork.pin` + one live pending proposal.
  - **Why the pin lands last:** once `demo/groundwork.pin` exists, `demo/` is a governed root and every *addition* of a demo skill or rule is an escalating change the #18 tripwire requires a matching proposal for. Authoring the demo under that constraint would mean a proposal per file. Landing the pin after the content settles activates the tripwire on a stable instance — and from then on, changes to demo content genuinely need proposals, which is the demo working as designed. The 2.3e live proposal is both the tripwire's happy path and #4's rung-5 governance block.
  - **Instance-relative references (load-bearing, Slice 2.3a):** a skill's `ontology:`/`baseline:`, a proposal's `target:`, and a changelog's skill path resolve inside their **own instance**, not the validated root. Under #10 a company OS *is* the root of its own repo, so its files carry repo-relative paths; if `demo/` is to be the shape the generator writes `your-company/` in, it must carry exactly those paths. It also stops a demo skill borrowing an engine exemplar by climbing out — the demo has to be a complete company, not a folder using this one's homework.
  - **`check_hooks` stays root-only** — the action-class gate is one shipped artifact with one registration, not per-instance content; a per-instance version would invite a `demo/governance/hooks/` whose registration claim nothing could satisfy.

- **Demo canon name (Slice 2.3b, decided early because it is public and hard to change):** the fictional company is **Umbercress**, domain `umbercress.example` (RFC 6761 reserved TLD *and* canon-declared — belt and braces), phones in the `555-01xx` fiction range, IPs in TEST-NET. Web-searched before adoption: zero presence as a company, product, or anything else, unlike the discarded candidates (`Quillstone` and `Larkfield` are both taken). Person names remain the documented #16 ceiling — a real name looks exactly like a fake one, so that layer rests on the fixed canon plus maintainer review, not on a check.

- **2026-07-28 — Slice 2.3a landed + merged to `main`** (`1cd13f1`, pushed). `_instance_roots` plus five checks converted to per-instance loops; the 1.5d-ii nested-root deferral closed. 572 tests, engine output held at exactly `0 error(s), 7 warning(s)` in both modes, and the five-check SILENT probe fired on every converted check. **Codex ran three rounds.** Round 1 found three real BLOCKING holes in the conversion — `superseded_by` resolving against the root instead of the record's instance; the baseline allowlist reloading `.gitignore` from the nested instance, so a root-ignored record could still satisfy the provisioning gate; and a *dangling* content-dir symlink no longer marking an instance, silently losing the symlink ERROR. Round 2 confirmed those fixed and caught that **the round-1 fix itself used the *first* `memory` path component**, which reopened the same cross-instance hole one layer down (`memory/company/memory/`); it is now the parent of the **last** component. Round 3 confirmed everything and flagged one non-discriminating test.
  - **The lesson worth carrying:** *a fix for a scoping bug is itself a scoping decision, and can reintroduce the bug at a different depth.* Rounds 1 and 2 were the same defect class, with the second instance created by the first fix. When a repair picks a path component, ask which one and why — first-vs-last is not a detail.
  - Also confirmed: the review gate ran at full strength this time (the r1 fix went back through the same thread rather than shipping self-verified), which is the live evidence for the standing re-review question.

- **Memory ownership decided (Slice 2.3b, maintainer call surfaced 2026-07-28):** 2.3a left containment **asymmetric** — a root skill could cite `baseline: demo/memory/x.md` (because `_memory_record_files(root)` walks every nested `memory/`), while the reverse was blocked. **Resolved: close it symmetrically** — *a memory record belongs to exactly one instance, the parent of the last `memory` component in its path, and only that instance may cite it.* Fable recommended leaving it because nothing exercised it; that argument expires the moment `demo/` holds records (2.3c), at which point an engine exemplar could take its baseline from the fictional company's numbers and the provisioning gate would be satisfied by invented data. Cheap because 2.3a already computes exactly that rule for supersession after two review rounds — it gets extracted to `_memory_instance_base` and gains a second consumer. **One rule, one home, two consumers** is what makes "belongs to exactly one instance" true rather than merely intended. Counter-argument recorded: it forecloses a shared memory pool, which #10's one-company-one-repo model does not sanction anyway.

- **Canon shape (Slice 2.3b):** `demo/canon.md` carries the machine-readable allowlist in #11 frontmatter (`domains`, `external_domains`, `phone_range`) and the fictional world in prose — #16's "one file that is both human documentation and machine-checked truth", the same pattern as the #9 manifest and the #18 proposal file. **`external_domains` ships empty on purpose:** without a declared escape hatch, the first legitimate link to something real creates pressure to loosen the check, which is how a positive allowlist dies; with one, the demo starts at zero real domains and any addition is a visible, reviewable edit to the file that documents the world. The **person names are the stated ceiling, written into the canon itself** rather than only into `docs/known-limitations.md`, so a reader meets the limit where the names are.

- **2026-07-28 — Slice 2.3b landed + merged to `main`** (`6e536c8`, pushed). `_memory_instance_base` extracted and shared, closing the ownership asymmetry in both directions; the Umbercress canon; `check_synthetic_identifiers`. 610 tests, engine output unmoved at `0 error(s), 7 warning(s)`. **Codex ran three rounds, 12 blocking findings total.** Round 1's set is the instructive one: uppercase spellings evaded every extractor; **7-digit phones — including the canon's own `555-01xx` form — were never extracted at all, which made the plan's own fiction-phone test vacuous**; `your-company/demo/` was wrongly in scope; a `domains: com` entry would have laundered an entire TLD; and the README claimed unbuilt content in the present tense. Round 2 then found four regressions *in the round-1 fixes* (compact `4155552671`, compact 7-digit false positives, `.co.uk` multi-label laundering, invalid IP-shaped hosts falling through both rules).
  - **The lesson: a positive allowlist has two independent failure surfaces — what it allows, and what it *sees*.** Every round-1 finding but one was an *extraction* failure, not an allowlist failure: the check was correct about every identifier it found and simply did not find them. A test that asserts "this valid phone passes" passes trivially when no phone is extracted, so the vacuous-test trap and the extraction gap are the same defect wearing two faces. For any future scanner, test the **negative** direction first — plant a violation and prove it is caught — because that is the only assertion an empty extractor cannot satisfy.

- **Slice 2.3c is content-only** (Umbercress ontologies + org memory; no validator changes) and carries one genuine **ticket-vs-ticket conflict** to settle: **#4 specifies performance-review prep as `Motion: assist`, but #5's locked vocabulary is `automate`/`build`/`buy`/`hire`/`wait` and `MOTIONS` encodes exactly those five.** Reading implemented: `assist` is informal shorthand in a parenthetical that #4 immediately glosses as *"the skill assembles the evidence pack; evaluation itself is forbidden on the Owner's Card"* — a **scoping** statement, not a verdict. The Motion ladder answers *how the work gets done*; *how far the agent may go* belongs on the Owner's Card and in the constitution, where it is enforceable rather than merely labelled. So the record ships `motion: automate` with the boundary in the body, the card's `forbidden_actions`, and the rung-5 rule — the renewal-prep shape proven twice in Phase 2.2. The alternative (add `assist` as a sixth motion) is a schema change needing a `SCHEMA_VERSION` bump under the rule set in 2.2a, plus new pivot semantics, and it contradicts #5's "Motion is the pivot" framing. Flagged for the maintainer rather than silently resolved.
  - Two smaller shape calls: the demo's executive views **deliberately differ** from the engine templates (founder-led sales, outside counsel, a fractional CFO), which is what makes the "templates are a starting map, not a claim about your company" frame concrete; and **demo records name people while engine records name roles**, which also gives 2.3d's card-drift checks real names to compare.
  - **Finance reading (#4 "pre-marked Motion `wait` … worksheets empty"):** taken literally those pull against each other, since Motion is a deep field. The Motion pivot already resolves it — a `wait` record carries only the common core, which is *opinionated* (a scored verdict) and *shallow* (four answers) at once, and makes the demo the second place the non-automation branch runs on real content.

- **2026-07-28 — Slice 2.3c landed + merged to `main`** (`28d3eba`, `--no-ff`, pushed). Content-only, as planned: 24 files — `demo/ontologies/` (eight Umbercress executive views that deliberately differ from the engine templates, four automation-path deep records, three Finance `Motion: wait` records) and `demo/memory/` (the async-standups supersession chain, two at-risk-renewal records, four captured baselines, the live-only index). 610 tests unchanged (no code, so no tests), engine output held at exactly `0 error(s), 7 warning(s)` in both modes. The `Motion: assist` conflict was surfaced rather than silently resolved and is **still awaiting the maintainer's confirmation**: implemented as `motion: automate` plus a scoping boundary carried on the record body, the Owner's Card, and a constitution rule — not as a sixth motion, which would be a schema change needing a `SCHEMA_VERSION` bump under the rule set in 2.2a.

- **Slice 2.3d decisions — five calls, pre-made from the record and flagged for the maintainer:**
  1. **The rung-3 reminder is `permissionDecision: "defer"` plus the reminder text — never `ask`.** This is the first artifact in the repo that renders a *rung* as a *mechanism*, so the mapping is load-bearing for everything V2 makes runnable. The Claude Code hooks reference was fetched live on 2026-07-28 (**it has moved: `docs.claude.com/en/docs/claude-code/hooks` now 301s to `code.claude.com/docs/en/hooks`**) and confirms a fourth `permissionDecision` value, `defer` ("use normal permission flow"), plus `additionalContext` inside `hookSpecificOutput` and a universal top-level `systemMessage`. That gives the exact ladder mapping: rung 4 (hard-block) is `deny`, which `action_class_gate.py` already emits; rung 3 (reminder) is `defer` + reminder text — the permission outcome is untouched and only what the agent and the human *know* changes. Shipping `ask` would have been **rung inflation**: machinery claiming more authority than the rule was granted, which is precisely what the five-rung ladder exists to prevent. Residual risk, documented in the artifact: `additionalContext` is shown alongside `allow` in the reference example and not explicitly alongside `defer`, so the reminder is emitted twice — once as `additionalContext` (agent) and once as `systemMessage` (human, universal to all events). **Without the live fetch this would have shipped as a rung-4 mechanism wearing a rung-3 label** — the third time in this build that fetching the live contract changed the code.
  2. **The demo exemplar lives in `demo/governance/reminders/`, and ships no `settings.snippet.json`.** `check_hooks` is root-only by design (2.3a: "a per-instance version would invite a `demo/governance/hooks/` whose registration claim nothing could satisfy"), so anything the demo puts under `hooks/` gets zero existence-checking — and an unverified registration file is exactly the named-but-unwired guard this repo has refused twice (1.5b made `check_hooks` verify the registration; 2.1 made the §6 drift check structural). So: the directory is named after its **rung**, making no registration claim by its name, and the registration JSON lives in a fenced block inside the exemplar's README next to the verification command. The README links the script with a relative Markdown link, so `check_links` — the one gate that does reach this directory — catches path rot. Cost accepted: a fenced block is less copy-pasteable than a file.
  3. **Performance-review prep is `reversible-write`, not track 2.** Classification in this repo has always been **by mechanism** (2.2a: "the classification is only as honest as the scoping"): the skill files an evidence pack the manager fetches, nothing is sent, nothing reaches the employee, and a wrong pack is regenerated. The rejected argument — that handing a manager attributed feedback about a named person is not "reversible" in the sense that matters — is a **harm** argument, and letting harm drive an action-class taxonomy would eventually reclassify everything sensitive as track 2 and destroy the distinction. The restraint is carried where it is enforceable: the card's `forbidden_actions` and the rung-5 rule. Cost is near-zero either way, since a blank track-2 field WARNs even on track 1, so the card fills all three regardless.
  4. **Three rules on three rungs, one of which declares a repeal.** Reminder / hard-block / human-decision, each bound to a real boundary in a real card so no rule is decorative. The meeting rule declares `repeals` with a `reassigned_to`, putting **orphan-prohibition on real content for the first time** — every prior exercise of that branch was a fixture. Same logic as 2.2b's `Motion: hire` record: a branch nothing real has run is a branch you do not know works.
  5. **One deliberate exception to "content slices change no code": the zero-dep test is widened.** `AGENTS.md` claims "every shipped script imports the Python standard library only", but `TestZeroDep` only scans `governance/hooks/*.py`. This slice ships the repo's first Python file outside that directory, so the claim would become true-by-luck. The fix renames the test and widens the glob to every shipped `.py` — no new test (the suite stays at 610), no `validate.py` change — with an explicit `assertIn` on the three known scripts so an empty scan cannot pass vacuously. **The generalizable rule: when a slice ships the first artifact of a kind an existing check names but does not reach, widening that check is part of the slice, not a follow-up.** A scoped check plus a broader written claim is trust debt with a delayed fuse.

- **2026-07-28 — Slice 2.3d landed + merged to `main`** (`9982f8e`, `--no-ff`, 16 new files + 3 modified). The four Umbercress work packages, the three-rule constitution (reminder / hard-block / human-decision, one declaring a repeal), and the meeting-challenger runnable exemplar — **#8 item 3 is closed**. 610 tests unchanged (the zero-dep test was renamed and widened, not added), engine output held at `0 error(s), 7 warning(s)`. Codex ran two rounds. Three results worth carrying:
  - **A fetched page is not a quotation — and this is a real qualification on the "read the live contract" move.** Four readings of the Claude Code hooks page disagreed about interactive behaviour, because **the page truncates on fetch before the PreToolUse section**, so every "verbatim quote" in review rounds 1 and 2 — the builder's *and* Codex's — had been reconstructed by a summarizing model rather than read. The exemplar now claims only what all four readings agree on and points readers at the live reference for the rest. The generalizable rule: fetching the live contract is still right (it is what produced the `defer` mechanism, and **the mechanism never depended on the disputed detail**), but a fetch result is *evidence about* a document, not the document. Quote it in a shipped artifact only when the fetch actually returned the section, and when it did not, ship the claim at the strength the evidence supports.
  - **`demo/` cannot cite any external URL, and that is now a standing authoring constraint.** Writing the caveat above tripped `check_synthetic_identifiers` — a `code.claude.com` link inside a demo file, against a canon whose `external_domains` is empty. The check was right; the wording changed rather than the canon. Decided in 2.3e (below): the escape hatch stays empty, with the reason written into `canon.md` where a reader meets it.
  - **`check_entropy` false-positives on long hyphenated path components** (a 40+ char run of `[A-Za-z0-9+/=_-]` at ≥ 4.0 bits). It forced two naming decisions in 2.3d. Any new deep path or filename gets checked before it is committed — and it is why 2.3e's pin carries a *short* commit sha.
  - Deliberately left by the maintainer: the duplicated stdlib allowlist across the two `TestZeroDep` methods, and the `demo/README.md` directory-link residual (path rot verified live by rename-probe).

- **Slice 2.3e decisions — five calls, pre-made from the record and flagged for the maintainer:**
  1. **The live proposal ships PENDING and UNAPPLIED; the tripwire's happy path is proven by probe, not by shipping the applied change.** The tempting reading of "one live proposal = the tripwire's happy path" is to commit the escalating change *and* its proposal together so `--diff main` exercises the matched path for real. Rejected: `proposals/` is **pending-only** (#18 — on apply the file evaporates into the consent commit), so an applied change sitting next to a still-`pending` proposal is a permanent self-contradiction in the demo, teaching the wrong lifecycle to every reader. Instead the committed state is honest (a proposal awaiting consent) and Task 4 proves **all three** tripwire behaviours by probe: missing-proposal ERROR, matched-proposal **silence**, and declared-vs-actual mismatch ERROR. That is strictly more coverage than the applied-change version would have given. **Method note worth keeping: Probe B asserts the *absence* of a finding, which is meaningful only because Probe A proved the same code path is live and loud on the same file class — an absence assertion is worthless alone and load-bearing in a pair.**
  2. **`external_domains` stays empty (Fable's 2.3d question, answered).** 2.3b's record anticipated this moment — the hatch exists so the first real link is a *visible, reviewable edit* rather than pressure to loosen the check — so populating it would be legitimate. Recommended against anyway, on a demo-side argument rather than a check-side one: **Umbercress does not use Claude Code; groundwork does.** A harness URL inside `demo/` is engine machinery leaking into the fictional company's records, and the engine artifacts that carry those URLs are not identifier-scoped. Honest cost: a real company OS links vendor docs constantly, so the demo models a habit that does not survive contact with reality — mitigated in one line rather than by loosening anything, with `canon.md` stating that the hatch is empty *because the company is fiction* and that a real repo populates it.
  3. **The pin's `generated_by_commit` is the SHORT sha, and that is a gate decision.** A full 40-char hex sha is exactly the shape `check_entropy` matches, sitting on the 4.0-bit threshold — whether it WARNs depends on that particular sha's digit distribution, which would make the 7-WARN invariant a coin flip per commit. `check_version_pin` requires only non-blank, and `MIGRATIONS.md` calls the field "provenance only", so nothing depends on the length.
  4. **This slice expires `MIGRATIONS.md`'s v1 exception, and the document must say so.** The 2.2a grammar tightening is justified there by "no `groundwork.pin` existed yet", with "the promise binds from the first real pin onward." **2.3e creates the first real pin**, so that paragraph becomes false on merge unless it is rewritten in the past tense with the date the promise began binding. The generalizable shape: **a justification written as a present-tense condition acquires an expiry date the moment something can satisfy it** — when a slice creates the thing a prior exception said did not exist, retiring that exception is part of the slice.
  5. **The rung-5 block is stated as instruction-strength, in the query itself.** #4 says the block "fires in-query", and it does in the sense that matters — the rule is discoverable from the skill and its card, and an agent that has read the repo refuses and cites rule, owner, and appeal. But it is not a runtime hook (#8 ships exactly one runnable exemplar, and it is the rung-3 reminder). The walkthrough says so at the emotional peak rather than in a footnote, because that is the moment a skeptical reader is looking hardest and the overclaim would cost the most.

- **2026-07-29 — Slice 2.3e landed + merged to `main`** (`098712d`, `--no-ff`, five slice commits). **Phase 2 is closed.** The 15-minute three-query walkthrough, one live pending proposal (`demo/proposals/refusal-names-next-step.md`), and `demo/groundwork.pin` — so `demo/` is a **governed root** under the #18 consent gate, `check_version_pin` runs on real content, and `MIGRATIONS.md`'s "no adopters yet" exception is **spent**: the pull promise binds from `14d164d` onward, and any future grammar tightening is a v2 change with a migration note no matter how small. 610 tests unchanged, `0 error(s), 7 warning(s)`, `--diff` exit 0. Codex ran one round (12 findings, all resolved in `8cec9fd`, re-review clean). Four results worth keeping:
  - **The plan's own tripwire demo was subtly wrong, and the correction is a real property of the gate.** The walkthrough said "change any rule and watch it ERROR" — false for `assessment-is-human-owned.md`, because **the tripwire matches on `target` + declared `blast_radius`, not on diff content.** A pending proposal therefore licenses *any* escalating edit to its target, not only the diff it printed. That is the honest shape of #18's check (it stops a rule edit smuggled under a track-1 label; it does not verify the edit matches the proposed text — that is the commit bit's job), and the walkthrough now names an *unproposed* rule for the red demo and shows the licensed silence separately as the consent gate's happy path.
  - **Two plan defects adapted during the build:** the rule file is `assessment-is-human-owned.md`, not the plan's longer `performance-assessment-…` spelling, and the plan's `meeting-challenger/README.md` links would have broken the 7-WARN invariant — that path run scores **4.108 bits**, over `check_entropy`'s 4.0 threshold. Both kept the directory-link form. **The entropy false-positive on long hyphenated paths is now a standing authoring constraint, and it is why 3.1's filenames are short.**
  - **A stale gloss in `docs/known-limitations.md`** ("pin → generated company content") predated the slice but became false the moment `demo/` got a pin; fixed in place because the file was already on the modify list. The general shape is 2.3e design call 4 one layer down: *a document that describes the world in the present tense acquires an expiry date the moment the world can change under it.*
  - Tooling: the Codex rescue forwarder backgrounds its task and loses the result, costing an hour mid-session; the workaround (`--wait`, synchronous run, companion `cancel` for stale locks) is saved to the maintainer's agent memory.

- **Phase 3 re-ordered (recommended in the Slice 3.1 plan, awaiting the maintainer).** The spec sequences Phase 3 as 3.1 protocol + state → 3.2 generator → 3.3 question skeleton. The recommendation is **3.1 state format → 3.2 protocol + question skeleton + constitution worksheet → 3.3 generator**. Reason: a consultant protocol without a question bank is inert prose — §4's four mechanics say *how* to ask and the 9-section skeleton says *what* to ask, so splitting them ships one slice nobody can use and forces `AGENTS.md` to describe a half-built interview on the surface where the honesty rule is hardest to hold. The state format, by contrast, is genuinely separable (a schema, two checks, a worked example), and both the protocol and the generator write to it, so building it first stops either of them inventing its own. Counter recorded: #9's decision is *about* the protocol's resumability, and the promote-and-commit step is a protocol rule — answered by shipping that state machine in `interview/README.md` alongside the schema, leaving only the interviewing *style* and the questions to 3.2. Total Phase 3 scope is unchanged; only the cut lines move.

- **Interview-state shape (Slice 3.1) — four calls, pre-made from the record and flagged:**
  1. **The manifest's "layer table" is a frontmatter list with NO status column.** Two decisions in one. *Frontmatter, not a table:* this repo has exactly one canonical table grammar, it cost 32 review rounds, and it governs `_executive-view.md` alone — a second table surface either reuses a grammar built for something else or restarts that treadmill, whereas a `layers:` list uses the #11 reader that has been stable since Slice 1.1 and stays bounded by layer count rather than transcript length. *No status column, and this is the load-bearing half:* a table declaring `confirmed | in flight` per row would be **precisely the agent-honored label #9 rejected Shape A for**. Status is derived from structure — a numbered file, listed in the manifest, carrying `provenance: confirmed`, committed. **The manifest points; it never labels.**
  2. **Interview state is discovered by CONTENT — a directory carrying `00-manifest.md` — never by directory name.** The engine ships `interview/README.md` (the format spec) and a company repo ships `interview/00-manifest.md` (the state); same name, different things, and a name-based check would scan the spec as if it were state. This is the doctrine already in the repo (#8's worksheets are silent by location; #5 is silent by absence). Consequence, stated up front: the example manifest lives in a **fenced block** inside the spec, never as `templates/00-manifest.md` — a template that looks like state is a trap, the same reasoning that kept `settings.snippet.json` out of `demo/` in 2.3d.
  3. **`demo/interview/` ships as the worked example, which answers the #9 fog question in the "retained" direction — for the demo.** Interview state is the provenance a future re-interview merges against (#9's own words), and a company that discards the record of how its OS was decided has discarded the answer to "why is renewal prep scoped this way." Recorded as the demo's *disposition*, not a rule: #10 guarantees interview state is never **distributed**, which is a different question from whether it is **retained**, and nothing in the validator requires an `interview/` directory to exist. Without it the check ships as a scan that scans nothing.
  4. **A third `--diff` pass turns `main()`'s pairwise dedupe into an accumulator.** The current code dedupes the blast pass against the memory pass because both resolve the git context independently and a fatal context ERROR would otherwise print twice; a third pass makes that form wrong. Replaced with a `seen` accumulator each pass filters against, plus a regression asserting an unknown ref yields **exactly one** context ERROR. Flagged because it edits working code no failing test currently covers.
  - **Named cut line:** the `--diff` frozen-layer guard (Task 4) is the half that makes #9's decision *true* — without it "confirmed" is a word a resuming agent can edit, which is what Shape A was rejected for. If it cannot land in the session it becomes a named **Slice 3.1b**, never a silent drop.

- **2026-07-29 — Slice 3.1 landed + merged to `main`** (`73f2c5e`, `--no-ff`, six commits). #9's resumable interview state, in full: `interview/README.md` (the format + the promote-and-commit protocol), `check_interview_state`, `demo/interview/` (five confirmed layers + one turn in flight), and the `--diff` frozen-layer guard with `main()`'s pairwise dedupe replaced by a `seen` accumulator. **662 tests** (610 → 662), `0 error(s), 7 warning(s)`, `--diff main` exit 0, `AGENTS.md` at 149 lines. All four anti-hollow probes fired. Codex: one round, 7 findings, no blockers — five fixed (two real check gaps: a `_working.md` naming a question while the manifest says none, and `status: complete` suppressing the open-question contradiction; three honesty fixes including a **pre-existing contradiction on `main`** where the review-prep ontology said "twenty evidence packs" against its own baseline's nineteen), two rebutted with evidence (an absent `layers:` key is the legitimate just-started interview, and symlinked state dirs are already caught by `check_symlinked_dirs`). Two results worth keeping:
  - **A new artifact can surface a contradiction that was already on `main`.** The demo's People/HR layer restated the review-prep numbers, and restating a number is how you find out the two records disagreed. Cheap, and only possible because the layer was written to *match committed content* rather than invented alongside it. Worth repeating deliberately: when a slice adds a record that re-states facts held elsewhere, the re-statement is a free consistency audit.
  - **The plan's live probes specified `--diff main`, but the layers did not exist at `main` yet** — the base-driven guard correctly saw only additions, so the builder ran them against `HEAD` instead and both fired. The general shape: *a probe for a diff-mode guard has to name a base where the thing being guarded already exists*, or it tests the addition path and reports success. **Open for the maintainer:** Codex's finding 1 — whether an absent `layers:` key should ERROR rather than read as an empty just-started interview.

- **Phase 3 re-ordering treated as accepted** (the 3.1 merge landed the state format first). Running order is **3.1** state format → **3.2** consultant protocol + question skeleton → **3.3** the generator. Say so if that is not the intent.

- **Slice 3.2 decisions — five calls, pre-made and flagged:**
  1. **The 9-section spec was read from the live `intent-engineering` tool, and the brief's paraphrase lists only seven.** Brief §4 enumerates "objective, outcomes, health metrics, constraints, autonomy, edge cases, stop rules"; the actual scaffold (generated 2026-07-29) is Objective / **User goal** / Desired outcomes / Health metrics / **Strategic context** / Constraints / Decision authority / Edge cases / Stop rules & verification. Both missing sections map onto fields groundwork already collects — user goal onto `card:job` and the ontology's accountability paragraph, strategic context onto Substrate / `gate_inputs` / `gate_source_of_truth`. Recorded because the brief is a locked source and the skeleton ships all nine on the strength of the live tool. **Third time in this build that fetching the live contract changed the artifact** (the hook contract, `AGENTS.override.md` precedence, and now this).
  2. **The nine sections map onto groundwork's existing fields almost one-for-one, so the skeleton ships as a question → destination-field map.** Two consequences. *The generator becomes mechanical* — every question already names the field it fills, so 3.3 is transcription plus formatting rather than interpretation. *The skeleton's completeness becomes testable* — a test asserts every field in the schema's required sets is named by at least one question, which is **#5's "all eight Gate fields must be answered; there is no waiver mechanism" enforced at the interview end** rather than only at the validator end. An interview that cannot fill a required field is now a failing test instead of a discovery made during generation.
  3. **Health metrics is the one section groundwork has no field for, and the schema is NOT bumped for it.** The spec's health metric is the Goodhart guard — *what must not degrade while the standard is met* — and groundwork has `success_standard` (what good looks like) and `known_failure_modes` (what has gone wrong) but no home for it. The question gets asked either way; the choice is whether the answer is a checked field or checked prose. Landing it in prose, because a new required field is now a **v2 bump with a migration note** (the pull promise began binding 2026-07-29), and spending the first-ever migration on one card field is a worse trade than a documented gap. Recorded in `docs/known-limitations.md` as the **first candidate for a v2 schema change**. Counter recorded: a field the validator does not know about is a field the generator can quietly skip.
  4. **The interview ships as documents, not as a skill — and it mechanically cannot be one.** `_check_owner_cards_instance` requires every `skills/<name>/` to carry a `SKILL.md` whose `ontology:` resolves under `ontologies/`, and the interview has no ontology deep record because it is not a company work package. It is also the wrong category: skills are what groundwork *generates for a company*; the interview is what generates them.
  5. **The no-human rule, answered in the strict direction.** §4's "no generation until understanding is complete" is a rule about sequence and leaves the headless case undefined — and an unconditional ask-the-human step in a chain ending in a subagent either hangs or is silently skipped, after which the agent proceeds on inference and calls it confirmed. So: **an unanswerable question halts the interview and is recorded in `_working.md`; it is never resolved by inference.** Same doctrine as #6's generator refusal, which is why owner, backup owner, forbidden actions, pause condition, and retirement condition are marked `(human-only)` in the question map — a mark 3.3's generator reads.
  - **Prose is the artifact with no gate, so this slice builds two tests for it:** the state-format doc's example blocks are extracted and run through `check_interview_state` (closing a gap 3.1 shipped — example files nothing checked), and the question bank is bound to `validate.py`'s own field constants in both directions. The question table is three canonical columns parsed with the **existing** `_canonical_row`, not a second table grammar.
  - **Named cut line:** if the question bank runs long it becomes Slice **3.2b**, and what defers is the coverage test plus the remaining rows — never the protocol/questions pairing, since splitting those re-creates the inert-prose problem the Phase 3 re-ordering existed to avoid.

- **2026-07-29 — Slice 3.2 landed + merged to `main`** (`84264f9`, `--no-ff`, five commits, PR #23). The consultant protocol (§4), the 9-section question skeleton as a question → destination-field map (51 canonical rows), the state-format doc's examples made executable, and the coverage test binding the bank to the schema in both directions. **665 tests**, `0 error(s), 7 warning(s)`, `--diff main` exit 0, `AGENTS.md` at 153 lines. **Codex: 9 findings, verdict do-not-ship — all real, all fixed in `3e8d456`, then a clean APPROVE on re-review.** Three worth carrying:
  - **The halt path stranded the thing it exists to save.** The protocol said an unanswerable question commits *the manifest*; `_working.md` stayed uncommitted, so a fresh checkout would fail `check_interview_state` — the very check the halt rule exists to satisfy. Now both commit together. The general shape: **a rule that writes state must name every file the state lives in**, or the rule's own invariant breaks at the next clone.
  - **The coverage test was quietly narrower than the validator.** The required direction omitted `card:action_class`, `rule:action_class` (which drives the no-rung-six safety spine), and `exec:activity`/`exec:direction`. A coverage test that does not cover what the gate requires is the vacuous-test trap wearing a new hat — it passes, and it proves less than it claims. Fixed with three new probes proving each addition is load-bearing.
  - **Three documents told three different built-status stories** (`interview/README.md` still said the protocol was not built; the protocol used present tense about the 3.3 generator; the `AGENTS.md` heading said the whole interview was not built). **Honesty is a cross-file property, not a per-file one** — when a slice moves something from not-built to built, every file that mentions it moves together.
  - Maintainer-accepted judgment call: the "batched questions" finding was reconciled with an ask-in-parts rule in `questions.md` rather than physically splitting multi-field table rows, to avoid changing the locked table shape for what is a protocol-level instruction. Tooling: the Codex stall recurred even with `--wait`; the recovery path (cancel the job, `task --resume-last` with a no-wait-tool nudge) plus a background watcher tripping on completion / 6-minute log silence / a 45-minute cap is now in the builder's memory. The re-review ran clean in 7 minutes, so the app update appears to have cured the hang.

- **Slice 3.3 decisions — five calls, pre-made and flagged. Two came from running the validator during planning rather than from reading the sources:**
  1. **The generator is a protocol an agent follows, not a script — and this is forced.** A `generate.py` would have to extract forty fields from **prose written by a human and an agent in conversation**, which is exactly the wrong-kind-of-parser trap 2.2a diagnosed: a restricted grammar is right for input groundwork's generator writes, and a parser is wrong for input it does not control. 3.2's question → field map is what makes agent-transcription reliable rather than creative.
  2. **The generator does NOT copy the runnable hook set into the company repo.** #8 calls the action-class gate "an artifact you install into a company repo," but #10's load-bearing promise is that upstream improvements arrive by `git pull` and **nothing is ever re-copied** — and a copied `action_class_gate.py` goes stale silently, which is the named-but-unwired-guard failure one level up: a company running last quarter's gate while believing it runs the engine's. So the generator writes `governance/review-gate.md` (the #19 prose form, already the documented degradation for every non-Claude harness) and installing the runnable gate becomes a documented maintainer act in `delivery/` with the re-copy obligation attached. **Honest cost, stated: a freshly generated OS has review-gate enforcement, not runtime enforcement, until someone installs the gate.**
  3. **`demo/` is not self-contained — four links escape into the engine** (`demo/README.md` → `../AGENTS.md`; `demo/canon.md` → `../docs/known-limitations.md`; `demo/governance/README.md` → `../../governance/README.md`; `demo/skills/README.md` → `../../skills/work-package-spec.md`). Found by scanning during planning. Each is *pedagogically* right — the demo teaches by pointing at the convention it instantiates — and would not resolve in a generated company repo. **Resolution: keep them, and pin the set with a test.** A fifth escape fails the build, so making the reference target less liftable becomes a decision somebody makes rather than a thing that happens.
  4. **`check_company_root` is a WARN, not an ERROR.** Verified during planning: **`python3 scripts/validate.py demo` prints `0 error(s), 0 warning(s)`** despite that tree having no root instruction file, no `CLAUDE.md`, and no Cursor pointer — because every root-only check is deliberately *silent on absence* (`check_root_files`: "Silent when there is no AGENTS.md"; `check_hooks` returns nothing for an absent hook set). They check a claim you make, not one you failed to make. The narrow fix: a root carrying a `groundwork.pin` **is** a company repo, so a missing root `AGENTS.md` WARNs — one rule, closing the entry point, since §6's rest already chains off AGENTS.md's presence. WARN because a validator cannot tell a half-generated repo from a deliberately minimal one, and because inverting a documented silent-on-absence posture would break `validate.py demo`. **The engine root has no pin, so the 7-WARN invariant must not move — if it does, the check is finding `demo/groundwork.pin` instead of a root pin, which is the 2.3a scoping-bug class.**
  5. **The end-to-end test is the slice's real deliverable: the first time `validate(root)` runs with `root` ≠ this engine.** `demo/` has only ever been validated as a *nested instance*, so `check_root_files`, `check_hooks`, `check_agents_chain`, and `check_always_loaded_budget` have **never run against company-shaped content**. The test copies `demo/` to a temp dir, adds the root files the manifest specifies, neutralizes the four known escapes, and asserts zero ERRORs — plus a companion test that breaks `CLAUDE.md` and demands the specific ERROR, because zero findings from a check that never looked is indistinguishable from zero findings from a clean repo. **Named cut line:** Task 3's check becomes Slice 3.3b if the session runs long; Tasks 1 and 2 do not split, because a manifest nothing materializes is a promise.

- **2026-07-29 — Slice 3.3 landed + merged to `main`** (`8c41721`, `--no-ff`, eight commits). **Phase 3 is closed.** `interview/generate.md` (four preconditions, the company-repo manifest, dependency-ordered generation, the five human-only refusals, the links-only-inside-itself rule), `check_company_root`, and the end-to-end proof — `TestGeneratedCompanyRepo` materializes a company repo and validates it as its own root at **0 ERRORs / 0 WARNs**, the first time the four root-only checks ever ran with `root` ≠ this engine. `TestDemoIsLiftable` pins the four engine-escaping demo links. **676 tests**, `0 error(s), 7 warning(s)`, `--diff main` exit 0, `AGENTS.md` at 152 lines. Codex ran **four rounds** (r1: 7 findings, 3 blocking; r2/r3 residuals; r4 APPROVE). Three results worth keeping:
  - **The fixture had to become the *post-generation* shape, not a copy of the demo.** The terminal fix was deleting the demo's five narrative files from the copy: **any retained narration can contradict a generated repo.** A fixture that is "the demo plus some root files" is not the artifact the manifest describes, and the difference is where a false pass hides.
  - **The escape tripwire needed to see link forms the obvious regex misses** — reference-style, angle-bracket, and list-nested — while ignoring fenced examples. Same lesson as 2.2b's `]:` ban: *a scanner's two failure surfaces are what it allows and what it SEES*, and the second one is where the vacuous pass lives. One nit accepted and commented in the test: escaped-backtick links stay invisible, inherited from `_strip_code`'s documented over-strip bias.
  - **A builder scope deviation that was right:** `interview/protocol.md` was edited although it was not on the plan's file list, because two "the 3.3 generator is not built yet" sentences would have contradicted `generate.md` in the same directory — the one-story failure Codex found in 3.2. **When a slice moves something from not-built to built, the file list must include every file that mentions it; a plan that omits one is asking the builder to ship a contradiction or go out of scope.** Deferred debt: `demo/README.md` still called the interview unbuilt, and `demo/` was untouchable in that slice — paid in 4.1.
  - Housekeeping: issue **#10** is covered by 3.3 + 4.1 and needs closing by hand.

- **Slice 4.1 decisions — five calls, and the live surfaces corrected two locked sources:**
  1. **The brief's "zip → org skills" is wrong in a way that changes the artifact: the unit of provisioning is a PLUGIN, not a skill.** Fetched 2026-07-29 (the plugin-marketplace reference plus the organization-plugins admin article). Skills reach an organization only *inside* a plugin, whose skills load from `skills/<name>/SKILL.md` under the plugin root. The manual path is a **plugin zip under 50 MB**, max **100 plugins** per manual marketplace, same-name uploads overwrite silently; the synced path is a **private** GitHub repo, max **500**. Both are **Team/Enterprise, Owners only**, and the prerequisite is stated flatly: *"Cowork and Skills must both be enabled for your organization before you can use plugin marketplaces."* That last line makes **#10's "the company repo doubles as the Cowork marketplace" first-party confirmed**, with Cowork as the gate rather than an alternative path. The spec's "org-skills zip" survives as intent, not as mechanism.
  2. **#10's confidentiality guarantee is conditional, and the condition was never written down.** "Only manifest-listed plugin directories are copied" is still true — but a `marketplace.json` entry with `"source": "."` makes the manifest-listed directory the **whole repo**, shipping `interview/` (the company's own transcript), `memory/`, and `governance/constitution/` to every employee who installs. The failure is silent, it is a privacy breach rather than a broken build, and **the natural first attempt — point the marketplace at the repo you already have — is exactly the wrong one.** So the guide states it as a hard rule up front: a plugin source is always a subdirectory. **No check can enforce it**: the manifest lives in a dot-directory `iter_files` never scans.
  3. **A generated company repo's skills load in NO harness until provisioning runs.** Confirmed against the live discovery table: Claude Code reads `.claude/skills/` and **`.agents/skills/` does not appear in it at all** (matching #19's 2026-07-18 A/B), while Codex/Cursor/Gemini read `.agents/skills/`. Nobody reads plain `skills/`, which is where 3.3's manifest puts them. That is the worst silent first-run failure available — generate a validating OS, open an agent, nothing happens, conclude groundwork does not work — so `check_company_root` gains **one** WARN (not one per skill: four cry-wolf lines on a demo nobody is provisioning is how a validator teaches people to ignore it).
  4. **A scope *reduction*, from a live fact.** The brief wanted "curated core default-on, rest available"; that is an **admin** setting with four values — Installed by default / Available for install / Not available / Required, plus Enterprise group overrides, and members cannot edit org-managed plugins. So groundwork's job is to **recommend** a curation, not implement one: everything *Available* to start, *Installed by default* only after someone chose it and an owner agreed to own it at scale, nothing *Required* on day one because a member cannot uninstall it.
  5. **No `marketplace.json` or `.claude-plugin/` ships in this repo — fenced blocks only.** Beyond the 2.3d precedent, the deciding reason is that a marketplace manifest is a **live external surface**: a real one committed here would make the groundwork engine itself a marketplace to anyone whose Claude Code discovered it, and because `iter_files` skips dot-directories the validator could never see it. **An artifact that changes an external system's behaviour and is invisible to our own gate is the one kind this repo should never commit as an example.**
  - **Named cut line:** Task 3's added WARN becomes Slice 4.1b if the session runs long; the guide and the honesty updates do not split.

- **2026-07-29 — Slice 4.1 landed + merged to `main`** (`5554b0b`, `--no-ff`, five commits). `delivery/README.md` (the repo-local symlink layer, org plugin upload, org GitHub sync, the runnable-gate install with its re-copy obligation), `check_company_root`'s harness-visibility WARN, the provisioning test, and the honesty updates. **684 tests** (676 → 684), `0 error(s), 7 warning(s)`, `--diff main` exit 0, `validate.py demo` = `0 error(s), 2 warning(s)`, `AGENTS.md` at 157 lines. The trigger check held — the new finding never fired on the engine root. Codex: one round, 11 findings (5 no-issues, 1 sandbox artifact, 5 should-fix), three fixed and re-review-confirmed. Three results worth keeping:
  - **A real bug in the plan's own check code:** an unlistable `.claude/skills`/`.agents/skills` left `visible=False`, so the validator emitted a WARN asserting a fact it could not inspect. Now an unlistable directory counts as **visible** — silence over false alarm — with a chmod-0 regression. Note the direction: this is the *opposite* of the usual fail-closed instinct, and it is right because the finding is a WARN about completeness, not a safety gate. **Fail-closed is a rule about safety checks; a diagnostic that cannot see should say nothing rather than accuse.**
  - **A same-length planted-violation probe poisoned `__pycache__`.** `skills`→`skillz` is byte-identical in length, and break-then-restore landed inside one mtime window, so stale bytecode survived while the source on disk was correct — the next full run failed a test the source could not explain. **Any planted-violation probe that makes a same-length edit must run under `PYTHONDONTWRITEBYTECODE=1` or clear `__pycache__` after restoring.** Saved to the builder's memory; worth putting in every future probe.
  - **Two findings deliberately deferred to the maintainer rather than fixed, because the kickoff locked the file list at five** — and that was the right call, since fixing them silently would have made the diff stop matching the plan. Both are now on the open list below.

- **Open for the maintainer (carried into Phase 4.2):**
  1. **`interview/generate.md:177` still says `delivery/` is "not built yet"** — stale as of 4.1. A one-word fix (drop ", not built yet"). This is the *third* time a slice has moved something from not-built to built and left a sibling file contradicting it (3.2's three-document split, 3.3's `protocol.md` deviation, now this). **The standing lesson is earned: a slice's file list must include every file that mentions the thing being promoted, and the plan is wrong if it omits one.** *(Settled in the 4.2 plan — Task 3 Step 5.)*
  2. **`AGENTS.md:44` says the symlink layer "makes generated skills loadable in all four harnesses"**, while `delivery/README.md` states the one-hop symlink shape is untested for Claude Code discovery (the empirically verified shape was two-hop). The builder recommends leaving it — the A/B did verify the layer in all four harnesses and the guide is the detail surface — with the honest counter that under a strict claims-match-verified reading "loadable in all four" is one hop ahead of the evidence. **Cheap either way; fold the decision into 4.2's README pass, which is the honesty slice.** *(Settled in the 4.2 plan — design call 2, resolved by changing the verb rather than the guide, after the two-hop alternative was investigated and rejected on evidence.)*
  3. **Issue #10 needs closing by hand** (`gh issue close 10`) — covered by 3.3 + 4.1. **Still open.**

- **Slice 4.2 decisions — five calls, and the fifth live-fetch correction of this build:**
  1. **Gemini CLI does not read `AGENTS.md`, so a Gemini user who points their agent at this repo today loads nothing.** Fetched live 2026-07-29, two independent first-party sources agreeing (the raw `google-gemini/gemini-cli` docs and the published docs site): the default context filename is `GEMINI.md`; `AGENTS.md` loads **only** when `context.fileName` is configured in `settings.json` — a setting no repository can supply for its readers; and the context file supports `@path` imports, "both relative and absolute paths," demonstrated as `@./file.md`. This lands squarely on 4.2 because "Not technical? Point your agent at this repo" is the README's *first* section and the gap falsifies that sentence. **Decided (maintainer, 2026-07-29): ship a one-line root `GEMINI.md` = `@./AGENTS.md` plus a missing-pointer WARN in `check_root_files`**, with exact precedent — that check already WARNs when no always-apply `.cursor/rules/*.mdc` references `AGENTS.md`. Honest cost recorded: 4.2 gains a `validate.py` change and a test in what would otherwise be a content slice, and it edits Slice 3.3's company-repo manifest (so the end-to-end fixture moves with it). Rejected: a content-only three-harness README with a Gemini footnote (fully honest, but leaves §6's root-file set incomplete against groundwork's own harness-agnostic headline), and shipping the file with no check (an unchecked pointer drifts, which is the failure `check_root_files` exists to catch for `CLAUDE.md`). Two sub-decisions: the shipped content carries the `./` prefix because that is the form the docs *demonstrate* (2.3d's rule — ship the claim at the strength the evidence supports), while the check accepts either spelling, because the invariant is "this file imports the canonical instructions," not "it is spelled the way we spelled it". **Fifth time in this build that fetching the live contract changed the artifact** (the hook output contract, `AGENTS.override.md` precedence, the `defer` rung-3 mechanism, the org-provisioning unit being a plugin — and now the fourth root pointer).
  2. **The `AGENTS.md:44` fix is a verb, not a guide rewrite — and the tempting alternative was investigated and rejected on evidence.** Switching `delivery/`'s commands to the "verified" two-hop shape does not work: the empirically verified shape (#19, 2026-07-18) had a **real skill directory** at `.agents/skills/<name>` with `.claude/skills/<name>` symlinked at it, while Slice 3.3's manifest puts skills at `skills/<name>/` — and it has to, because `iter_files` skips every dot-directory and skills under `.agents/skills/` would be invisible to the validator (the corpus-void trap). So *some* extra hop is unavoidable, no fully-verified zero-hop shape exists, and `delivery/`'s caveat plus its `/doctor` fallback is the honest artifact. `AGENTS.md` moves from an outcome ("makes generated skills loadable") to a structure ("gives generated skills a harness-visible path"), which is exactly what was verified. **The generalizable shape: when a claim and its detail surface disagree, check whether the "stricter" fix is actually available before choosing which one to weaken.**
  3. **The License carve-out promise was not merely due — it was unpaid where it mattered.** The README promised "an explicit README/NOTICE carve-out will ship with the generator"; the generator shipped in 3.3, and reading `interview/generate.md` during planning showed **it says nothing about licensing at all**, so a generated company repo carries no statement that its content is the company's own. The README's own sentence is substantively the carve-out, but the promised artifact — one that ships *with the generator* — did not exist. **Decided (maintainer, 2026-07-29): pay it in 4.2** — one sentence in `generate.md`'s root-files step, and a README License section that describes what exists and names 4.3 for the `LICENSE` file rather than making a second promise. Third occurrence of the shape the log has already named twice: *a justification written as a present-tense condition acquires an expiry date the moment something can satisfy it* — "will ship with the generator" expired on `8c41721`.
  4. **The promotion sweep is a pre-built table this time, not a post-hoc discovery.** Three consecutive slices shipped a sibling file contradicting a promotion (3.2's three-document split, 3.3's `protocol.md`, 4.1's `generate.md`), so the plan carries a grepped eight-row table of every file mentioning something 4.2 promotes — `AGENTS.md` ×4 (the status paragraph is three phases stale, the `delivery/` verb, the Tier-2 bullet, the root-file sentence), `interview/generate.md` ×3, `docs/known-limitations.md:78`, and the end-to-end fixture — **plus a closing grep that fails the sweep if any survive.** Nothing under `demo/` appears; checked, not assumed.
  5. **In the honesty slice the risk inverts: the failure mode is a true qualifier smoothed away, not a false sentence added.** Three hedges are named in the plan's Global Constraints as load-bearing understatements that must survive editing — the demo's rung-5 refusal is **instruction-strength, not a runtime block**; skill auto-invocation is **not reliable**; the generator is **documents an agent follows, not a program**. The README also ships with **no test count, star count, or commit sha**, because a number that goes stale every slice is trust debt with a timer. Every capability sentence is bound to a file or a test in a nine-row anchor table, and the rule is: a sentence with no anchor gets cut rather than researched.
  - **What must not regress, verified by grep rather than by reading:** #14's credit block stays at the bottom in prose bullets with paid/free stated straight, and #15's two-project comparison keeps its pointer-row table plus the "On the two active projects" prose that is the single source of truth for the concessions. A rewrite is exactly when a grilled concession quietly softens, so the plan diffs `README.md` for the named parties and expects zero hits.
  - **Named cut line, with its consequence stated:** Task 1 becomes Slice **4.2b** — and cutting it *changes Task 2*, whose step 1 must then name three harnesses plus the one-line Gemini fix, because **the README may not claim the fourth pointer before the file exists.** Tasks 2 and 3 do not split.

## Success criteria for the V1 build

- The adopter's required path runs end-to-end: `interview/` generates a `your-company/` in the demo-proven shape; `scripts/validate.py` passes on it; a provisioning guide exists.
- `demo/` runs the 15-minute 3-query script with zero credentials, including the rung-5 governance block.
- `scripts/validate.py` is Python-3-stdlib-only (zero third-party deps; self-checked), and every #5/#6/#7/#8/#16/#18/#21 ERROR/WARN rule fires where CONTEXT.md says it should.
- The README is honest at every stage: no capability claim precedes the capability; positioning concedes shared ground; prior art credits by name with paid/free stated straight.
- Root files match §6: `AGENTS.md` canonical, one-line `CLAUDE.md` import, `.cursor/rules/` pointers; drift between them is a validator check.
