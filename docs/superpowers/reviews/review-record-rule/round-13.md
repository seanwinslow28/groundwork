## Round 13 — 2026-08-29, record correction (not a review round)

No Codex invocation. This entry carries the reclassification round 12 finding 1 requires,
which cannot be made by editing the entries that hold those rows.

**Fix commit for round 12: named in the README's map.**

### Reclassified, superseding rounds 10 and 11

Rule 9 admits **fixed**, **rejected with grounds**, and **open**. "Standing" is not one of
them, and two rows used it:

| Entry | Row | Was | Now |
|---|---|---|---|
| round-10 | 6 | "Standing, unchanged" | **Open** — the session departs from rule 1, two increments in one, and the kickoff authorisation is not a repository artifact a reader can check. Listed in the README's open findings; ratification is the maintainer's at merge |
| round-11 | 5 | "Standing and unchanged" | **Open** — the same finding, restated a round later |

Both describe one thing: this session built two slices where rule 1 allows one. That was
disclosed from the moment it was noticed and is not reopened here. What changes is only that
the record now says **open**, which is the word rule 9 gives it, instead of a word rule 9
does not define.

### Why this is a separate entry

Rounds 10 and 11 are immutable once committed, so their rows cannot be edited to say "open".
Rule 9's answer is that a correction to an earlier entry is made by a later one — this is
that later one. It is numbered like any other entry and says on its first line that it is
not a review round, which is the same shape `round-07.md` used for the maintainer decisions.

### The complete disposition accounting, as it now stands

Seventeen historical rows were escalated rather than closed. Fifteen are **fixed**, with
their commits named across `round-09.md`, `round-10.md` and `round-11.md`. Two are **open**:
round-05 row 9 (branch renames and verdict vocabulary) and round-08 row 9 (five points,
including the clean-round form). The two rows reclassified above are **open**. No row on this
branch is rejected, and none is left in a vocabulary rule 9 does not define.
